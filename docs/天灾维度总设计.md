# 天灾维度总设计

> 状态：**现行实现**（基于 `09475b5`「第二阶段初步完成」，含 PR #6 的 Iris 兼容渲染改造、
> 第一阶段的 32 项 BWG 替换与第二阶段的 21 项自制群系接管）
> 覆盖范围：`beloong:disaster` 维度的定义、进入方式、群系构成与剔除、结构联动
> 本文取代此前所有分散的天灾相关设计/计划文档（见文末「已废弃文档」）
>
> **两条总目的**（§1.1）：① 用 BWG 的现成群系充实天灾维度，从而不必自制太多群系；
> ② 隔离原版结构与天灾结构——天灾维度不生成原版任何结构。
> 二者共同指向同一手段：**天灾维度的群系绝不能是原版群系本身**。
> **其中目的 2 由整合包负责达成，不在本模组的范围内**（§1.3）。
>
> **阶段说明**：
> - **第一阶段**：32 个原版群系已替换为 BWG 群系，21 个白名单原版群系保留。
>   （其中 5 项黑名单泄漏由第二阶段顺带修复，见 §10.6。）
> - **第二阶段（已完成并通过验收）**：21 个白名单原版群系全部接管 ——
>   14 项由 **5 个 `beloong:` 自制群系**覆盖（海洋 2 + 河流 1 + 洞穴 1 + 碎裂地形 1），
>   7 项交给 BWG。白名单已清空。
>   **实测账目：`参数点 7593 个 = 替换 7593 + 非原版保留 0 + 未能求解 0`；
>   BWG region 树的 `minecraft:` 群系为 0 种；天灾维度区块实测可正常生成。**
>   **客户端观感验收已通过**（碎裂地形露石而非草坡、冰海结冰、暖/热海洋风味均符合预期）；
>   验收会话的日志复核未发现任何由本模组引入的问题。
> - **目的 2 由整合包负责达成，不在本模组的范围内**（§1.3）。

---

## 一、设计目标

天灾维度是「化龍」整合包的高价值战利品区域，同时也是**独立的群系舞台**。

### 1.1 两条总目的

本文其余部分全部由下面两条总目的推导而来。任何与之冲突的既有设计都应视为需要修正。

**目的 1 —— 用 BWG 的现成群系充实天灾维度，从而不必自制太多群系。**

天灾维度需要一套与主世界**观感不同**的群系构成。BWG 提供 55 个现成群系，把它们整体迁进来，
就省掉了自行设计几十个群系的成本。**自制群系只用于填补 BWG 在参数空间上确实给不出东西的位置，
且数量越少越好**——自制群系每多占一个气候区，BWG 群系的权重就少一分。

**目的 2 —— 隔离原版结构与天灾维度的结构：天灾维度不生成原版任何结构。**

天灾维度承载 Cataclysm / FDBosses 的高难度结构（见第五节）。若原版结构（村庄、要塞、
试炼密室、海底神殿、远古城市、矿井、沉船…）同时生成，会把玩家的探索路线与战利品预期搅乱。
因此**天灾维度只允许生成非 `minecraft:` 命名空间的结构**。

> **两条总目的指向同一个手段：天灾维度的群系绝不能是原版群系本身。**
>
> Minecraft 的标签是按**群系 ID** 挂载的。把原版群系复制成 `beloong:` 命名空间后，
> 新群系**不会自动继承任何原版标签**——于是原版结构在这些参数区自然失去生成条件（服务于目的 2），
> 而我们也只需"复制 + 改名"而不必重新设计（满足目的 1）。这是第二阶段选型的核心依据。
>
> ⚠️ **职责边界：** 目的 2 是**整合包**的目标，由整合包层面达成；
> 本模组（BeLoong-Core）只负责**目的 1 对应的群系层面工作**
> ——即"让天灾维度的群系里不残留 `minecraft:` 群系"。详见 §1.3。

### 1.2 三条技术设计意图

1. **第二个主世界**：地形、结构、洞穴、矿物全部复用原版主世界，玩家不需要学习新的世界生成规则。
2. **群系全面替换**：Oh The Biomes We've Gone (BWG) 的生物群系**从主世界整体迁移到天灾维度**，
   并进一步**剔除天灾维度里不需要的原版群系**——主世界退回纯净原版群系。
   天灾维度最终为 **BWG 群系（94.9% 参数点）+ 5 个 `beloong:` 自制群系**，
   **不含任何 `minecraft:` 群系**（第二阶段已完成）。详见第四节。
3. **唯一的进出通道**：进出只能通过「天灾传送门」。传送门需要集齐 12 种来自 Cataclysm / FDBosses 的眼球，构成进入天灾的进度门槛。

设计意图 2 意味着：**在主世界禁用 TerraBlender 是刻意行为，不是缺陷。**

### 1.3 职责边界：目的 2 由整合包负责，不由本模组负责

> **本模组（BeLoong-Core）的交付物只有一件事：让天灾维度的群系里不残留 `minecraft:` 群系。**
> 「天灾维度不生成原版结构」这一最终效果由**整合包层面**达成，**不在本项目的范围内**。

把边界划清楚的理由与相关事实（供整合包侧参考，本模组不实施）：

- 1.21.1 **没有"按维度挑选结构集"的机制**：`ChunkGenerator.createState`
  （`ChunkGenerator.java:111-113`）把结构集注册表里**全部** structure set 交给每个维度，
  只用 `hasBiomesForStructureSet` 按**群系**筛一道
  （`ChunkGeneratorStructureState.java:52-67`）。因此"某原版结构会不会在天灾维度生成"，
  完全取决于它的 `has_structure/*` 标签里的群系有没有落在天灾维度的 `possibleBiomes()` 里。
- 当前两处来源都带着原版结构标签：

  | 来源 | 携带情况 |
  |---|---|
  | 白名单 21 项原版群系 | **全部**原版结构标签（`is_ocean` / `is_river` / `is_hill` / `deep_dark` / `is_overworld` / `has_structure/*` …） |
  | 全部 BWG 群系 | BWG **主动添加**，见 `TagsGenerator.java:478-485`；其中 `HAS_TRIAL_CHAMBERS` 与 `IS_OVERWORLD` 覆盖**全部** BWG 主世界群系 |

  第二条已用**运行时 jar** 逐字核实（BWG 2.6.0 的
  `data/minecraft/tags/worldgen/biome/has_structure/trial_chambers.json` 内容为
  `{"values":["#biomeswevegone:overworld"]}`）。

**第二阶段顺带产生的贡献：** 把 21 项换成 `beloong:` 群系后，这些群系**不继承任何原版标签**，
原版结构在那部分参数区自然失去生成条件。这**不需要额外工作**，是"复制原版群系并改名"的
免费副产品，但它**不等于**完成了结构隔离（BWG 群系仍带着原版结构标签）。

> **对本模组的约束（务必遵守）：** 接管白名单项时，**不要**把 `beloong:` 群系加入任何
> 原版 `has_structure/*` 标签——那会把这个免费的副产品破坏掉。

> **分阶段目标**
> - **第一阶段（已完成）**：天灾维度 53 个原版主世界群系中，32 个替换为 BWG 群系、21 个白名单保留；
>   被替换的 32 个从 `possibleBiomes()` 中消失（`/locate` 搜不到、自然罗盘只显示在主世界）。
>   **曾有的残留**：5 个黑名单群系（`badlands` / `eroded_badlands` / `wooded_badlands` /
>   `mushroom_fields` / `beach`）仍由 BWG 的 region 树生成，只是被查询层过滤器隐藏。
>   **该残留已在第二阶段随第三条注入路径一并修复。**
>   见 `docs/reviews/2026-09-11-disaster-region-tree-probe.md`。
> - **第二阶段（已完成并通过验收）**：21 个白名单项全部接管，天灾维度**不含任何
>   `minecraft:` 群系**。逐项方案见
>   [`docs/plans/2026-09-11-disaster-phase2-biome-table.md`](plans/2026-09-11-disaster-phase2-biome-table.md)，
>   执行记录见
>   [`docs/plans/2026-09-11-disaster-phase2-implementation.md`](plans/2026-09-11-disaster-phase2-implementation.md)。

---

## 二、维度定义

天灾维度是**纯数据包维度**，没有任何自定义 Java 维度类。

### `data/beloong/dimension_type/disaster.json`

| 字段 | 值 | 说明 |
|---|---|---|
| `natural` | `true` | 保留昼夜与天气 |
| `coordinate_scale` | `1.0` | 1:1 坐标传送的维度侧前提 |
| `has_skylight` | `true` | 有天空光 |
| `has_ceiling` | `false` | 无天花板（区别于下界） |
| `ambient_light` | `0.0` | 无环境光补正 |
| `bed_works` | `true` | 床可睡、可设重生点 |
| `respawn_anchor_works` | `false` | 重生锚不可用 |
| `has_raids` | `true` | 可触发袭击 |
| `min_y` / `height` / `logical_height` | `-64` / `512` / `512` | 与原版主世界一致 |
| `infiniburn` | `#minecraft:infiniburn_overworld` | 火焰蔓延规则同主世界 |
| `monster_spawn_light_level` | `uniform 0~7` | 原版主世界刷怪光照规则 |
| `piglin_safe` | `false` | — |
| `effects` | `minecraft:overworld` | **保留原版主世界天空与天气** |

**关于 `effects: minecraft:overworld`**：天灾维度**不**使用自定义天空盒。龙宫的 Dramatic Skys 天空渲染、雾处理、天气禁用等全部限定在 `beloong:loong_palace`，天灾维度不受任何影响。这是刻意的范围划分，避免天空系统与战利品维度的职责耦合。

### `data/beloong/dimension/disaster.json`

```json
{
  "type": "beloong:disaster",
  "generator": {
    "type": "minecraft:noise",
    "settings": "minecraft:overworld",
    "biome_source": {
      "type": "minecraft:multi_noise",
      "preset": "minecraft:overworld"
    }
  }
}
```

- `settings: minecraft:overworld` → 地形、洞穴、矿物、海平面、结构集全部与主世界一致
- `biome_source: multi_noise` → **这是 TerraBlender 能够介入的前提**（TerraBlender 只处理 `MultiNoiseBiomeSource`）
- 维度种子由存档种子独立派生，因此地形与主世界不同，但**生成规则相同**

### 维度 ID 的硬编码点

`DisasterPortalBlock` 中维度 ID 是编译期常量，**不受配置控制**：

```java
private static final String DISASTER_DIM = "beloong:disaster";
```

---

## 三、进入方式：天灾传送门

传送门模仿原版末地传送门 + End Remastered：**5×5 环形框架，12 个框架各嵌入 1 种不同眼球后激活**。

### 3.1 结构形状

```
?  v  v  v  ?      角落：任意方块（不参与匹配）
>  ?  ?  ?  <      四边：各 3 个框架，朝向指向结构内部
>  ?  ?  ?  <      中间 3×3：激活后填充天灾传送门方块
>  ?  ?  ?  <
?  ^  ^  ^  ?
```

用 `BlockPatternBuilder` 匹配（`DisasterPortalFrame.getCompletedPortalShape(boolean requireEyes)`）：

- `requireEyes = true` → 仅匹配 `EYE_TYPE != EMPTY` 的框架，用于**激活检测**
- `requireEyes = false` → 匹配所有框架，用于**去重检测**（框架可能在眼球未放满时就已搭好）

四个符号各自的朝向是固定的：顶边朝 SOUTH、底边朝 NORTH、右边朝 WEST、左边朝 EAST。`FACING` 在放置时取玩家水平朝向的反方向。

### 3.2 方块清单

| 方块 / 实体 | ID | 职责 |
|---|---|---|
| `DisasterPortalFrame` | `beloong:disaster_portal_frame` | 框架，`Block implements EntityBlock` |
| `DisasterPortalFrameEntity` | `beloong:disaster_portal_frame_entity` | 持久化眼球槽位编号 |
| `DisasterPortalBlock` | `beloong:disaster_portal_block` | 传送门本体，接触传送 |
| `DisasterPortalBlockEntity` | `beloong:disaster_portal_block_entity` | 纯渲染载体，无数据 |

**框架方块属性**：`strength(50, 1200)`、`requiresCorrectToolForDrops()`（下界合金镐）、绿色地图色、玻璃音效、发光 1、`pushReaction BLOCK`、掉落自身。

**BlockState 属性**（共 `4 × 2 × 13 = 104` 种组合）：

| 属性 | 类型 | 值 |
|---|---|---|
| `FACING` | `EnumProperty<Direction>` | `north` / `east` / `south` / `west` |
| `HAS_EYE` | `BooleanProperty` | `true` / `false` |
| `EYE_TYPE` | `EnumProperty<EyeType>` | `"0"`（空）+ `"1"`~`"12"` |

**传送门方块属性**：`strength(-1, 3600000)` 不可破坏、`noLootTable()`、`noCollission()`、发光 11、`RenderShape.INVISIBLE`（视觉完全交给 BlockEntity 渲染器）、碰撞箱 16×12×16。

### 3.3 EyeType 与配置的映射

`block/EyeType` 是 13 值枚举，实现 `StringRepresentable`，BlockState 序列化名即数字字符串：

```
EMPTY("0")
SLOT_1("1") ... SLOT_12("12")
```

映射规则：**`Config.DisasterPortal.eyeItems` 列表的位置索引 + 1 = 槽位编号**。

| 配置列表索引 | 槽位 | 枚举值 | 纹理 |
|---|---|---|---|
| — | 0 | `EMPTY` | `disaster_portal_frame_top.png` |
| 0 | 1 | `SLOT_1` | `disaster_portal_frame_top_1.png` + `_eye_1.png` |
| … | … | … | … |
| 11 | 12 | `SLOT_12` | `disaster_portal_frame_top_12.png` + `_eye_12.png` |

`registry/ModBlocks#getEyeSlot(ItemStack)` 负责查询：在配置列表中查找物品完整 ID，命中返回 `索引 + 1`，未命中返回 `0`。

> **为什么是数字槽位而非语义化命名**：早期实现用 `"cataclysm:mech_eye"` 等语义短键，导致枚举与硬编码 Map 绑定、改配置不生效。改为数字槽位后，配置列表里放任意物品 ID 都能工作，纹理由槽位编号决定。

### 3.4 激活流程

```
玩家手持眼球右键框架
  → HAS_EYE 已为 true？                        → PASS
  → getEyeSlot(stack) 返回 0（非有效眼球）？     → PASS
  → 客户端？                                   → SUCCESS（触发交互包发往服务端）
  → isEyeAbsent(level, pos, eyeType) 为 false？ → 提示「该眼球已放置在传送门中」→ FAIL
  → setBlock(HAS_EYE=true, EYE_TYPE=对应槽位)
  → BlockEntity.setEyeId(String.valueOf(slot))
  → stack.consume(1, player)
  → 播放 SoundEvents.END_PORTAL_FRAME_FILL
  → getCompletedPortalShape(true).find(level, pos)
      命中？→ 中间 3×3 填充 DisasterPortalBlock
            → globalLevelEvent(1038)（原版末地门激活特效）
  → CONSUME
```

**去重机制**：`isEyeAbsent()` 用 `requireEyes=false` 的模式定位 5×5 结构，遍历 25 格找出所有 `DisasterPortalFrame`，比较 `EYE_TYPE`。若模式未匹配到（框架不完整），返回 `true` 放行。

### 3.5 破坏行为

框架方块 `onRemove()`：扫描**以自身为中心的 7×7 水平范围**（覆盖整个 5×5 结构），对所有 `DisasterPortalBlock` 调用 `level.destroyBlock(pos, false)`。借原版 `destroyBlock` 免费获得与下界传送门破碎一致的音效和粒子。传送门方块本身不掉落任何物品。

### 3.6 传送逻辑

`DisasterPortalBlock implements Portal`，采用**原版传送门范式**：`entityInside` **只登记**，落点计算与换维度推迟到原版 `Entity.handlePortal()` 回调 `getPortalDestination()`，由 `DimensionTransition` + `postDimensionTransition` 完成。**传送门代码路径上不存在任何同步区块加载**（`Level#getChunk` / `Level#getHeight` 一律不用）。

```
entityInside（方块回调，由 Entity.move → checkInsideBlocks 触发；仅服务端、仅 ServerPlayer）
  冷却检查：player.getPersistentData().getLong("beloong_portal_cooldown")
            > level.getGameTime() ? 直接 return
  player.isPassenger() ? stopRiding()   // 原版 handlePortal 用 canUsePortal(false) 作闸门
  entity.setAsInsidePortal(this, pos)   // 只登记，零 I/O
  当前维度 ≠ beloong:disaster ? 非阻塞预热落点区块（只申领票据，不等待）

Entity.handlePortal() → PortalProcessor（getPortalTransitionTime() 返回 0 ⇒ 即时）
  → DisasterPortalBlock.getPortalDestination()
     ┌─ 当前维度 ≠ beloong:disaster（下行）
     │    X / Z 原样保留（1:1）
     │    chunk = targetLevel.getChunkSource().getChunkNow(x>>4, z>>4)   ← 非阻塞探测
     │    未就绪？→ addRegionTicket(TicketType.PORTAL, 落点区块, 半径 0, 门坐标)
     │              + player.setPortalCooldown(0)   ← 否则原版冷却会吃掉重试机会
     │              + return null（下一 tick 重试；超过 200 tick 则报错放弃）
     │    已就绪 → LevelChunk#getHeight(MOTION_BLOCKING, x&15, z&15) + 1 + 1
     │    → DimensionTransition(targetLevel, (x, 地表+1, z), 速度 0, yRot, xRot, 回调)
     └─ 当前维度 == beloong:disaster（上行）
          respawnPos = player.getRespawnPosition()      // 床 / 重生锚
          respawnDim = player.getRespawnDimension()
          无重生点？→ 回退 overworld.getSharedSpawnPos() + Level.OVERWORLD
                     → 最终兜底 new BlockPos(0, 64, 0)
          → DimensionTransition(targetLevel, (x+0.5, y, z+0.5), 速度 0, yRot, xRot, 回调)

postDimensionTransition 回调：
  entity.fallDistance = 0
  persistentData.putLong("beloong_portal_cooldown",
                         level.getGameTime() + Config.DisasterPortal.teleportCooldownTicks)
```

要点：
- **下行是 1:1 坐标映射**（靠 `coordinate_scale: 1.0` + 手动保留 X/Z）
- **上行照搬原版末地返回逻辑**，回到玩家重生点而非坐标对应点
- 朝向在双向传送中均保留；速度清零（等价旧 `teleportTo(..., Set.of(), ...)`）
- 冷却写入玩家持久化 NBT，跨维度/重登有效；键名 `beloong_portal_cooldown` **不得更改**
- 落点高度用 `LevelChunk#getHeight(MOTION_BLOCKING, …) + 1 + 1`，与旧 `Level#getHeight(…) + 1.0` **逐字等价**，但不触发区块加载
- 落点未就绪时的日志锚点（ASCII：实例日志是 GBK，中文锚点不便脚本判读）：
  `[BeLoongCore][DisasterPortal:wait]` / `[:teleport]` / `[:timeout]` / `[:error]`
- 客户端侧（2026-09-12 增补）：`entityInside` 在**客户端也登记** `setAsInsidePortal`，配合
  `getLocalTransition() = CONFUSION` 获得下界门同款的视角扭曲过渡（镜头扭曲 + 全屏紫幕 +
  `PORTAL_TRIGGER` 环境音、自动关容器）；进入/离开该维度时显示的「加载地形中」界面由
  `BeLoongCoreClient` 注册为天灾门专用背景（NeoForge `RegisterDimensionTransitionScreenEvent`）。
  详见 [`docs/plans/2026-09-12-disaster-portal-transition-design.md`](plans/2026-09-12-disaster-portal-transition-design.md)

> **为什么绝不能同步加载落点区块（2026-09-12 实机事故）**
>
> 旧实现在 `entityInside` 里直接 `targetLevel.getChunk(x>>4, z>>4)` 同步加载**另一个维度**的区块，主线程会停在 `ServerChunkCache.getChunk` 的 `managedBlock` 上自旋等待区块 future：
>
> - `managedBlock` 只能推进主线程自己的任务队列，**覆盖不到区块生成流水线与后台 worker 的协同**，而 future 的完成事实上依赖主线程继续跑 tick —— 互相等待；
> - NeoForge 的 `currentlyLoading` 旁路（`ServerChunkCache.java:154-155`）只在 **FULL 状态同步载入窗口**内有效，生成早期阶段失效；
> - 一旦 future 因异常逃逸而**永不完成**（实机：结构排除区成环使 worker 抛 `StackOverflowError`；环为 `beloong:disaster_set_ground` ⇄ `beloong:disaster_set_underground` 互写 `exclusion_zone`，整合包侧已修复；异常从任务逃逸、无 crash report），主线程就**永久静默卡死**（ModernFix 看门狗每 41 s 连抓三份完全相同的转储）。
>
> 因此改为：`getChunkNow` 非阻塞探测 + `TicketType.PORTAL` 区域票据后台加载 + 下一 tick 重试 + **200 tick 有界超时**（超时报错并提示玩家，绝不阻塞主线程）。
> 根因、取证与被否方案见 `docs/天灾传送门主线程死锁-根因与修复复盘.md`。

### 3.7 渲染

`client/DisasterPortalRenderer`（`@OnlyIn(Dist.CLIENT)`）是原版 `TheEndPortalRenderer` 的**完全重新实现**——原版渲染器在字节码层把 BlockEntity 硬转型为 `TheEndPortalBlockEntity`，无法靠子类化复用。

- **渲染管线**：`RenderType.entitySolid(beloong:textures/disaster_portal.png)`，即原版标准实体管线 `rendertype_entity_solid`。**不使用自定义着色器。**
- **顶点数据**：8 个浮点坐标、4 顶点绕序与原版 `TheEndPortalRenderer` **逐字相同**；每个顶点写入 color(1,1,1,1)、overlay、`LightTexture.FULL_BRIGHT`（与原版末地传送门一致恒全亮）以及该面的法线
- **UV**：在 Java 侧用**变换后的世界坐标**计算 `u = x/3 + 1/3`、`v = z/3 + 1/3`，与旧核心着色器的公式一致，使相邻传送门方块共享连续纹理坐标
- 6 面立方体一次提交；上下表面内收（`down = 0.375` / `up = 0.75`）形成收窄的立体感
- 每个面经 `DisasterPortalBlockEntity#shouldRenderFace(Direction)` 判定，仅 Y 轴方向可见（与原版一致）

> **为什么必须是标准管线（Iris 兼容性）**
>
> 本项目曾使用自研 `ShaderInstance`（`beloong:rendertype_disaster_portal`，`.vsh`/`.fsh` 照搬原版 `rendertype_end_portal`）。该方案在 Iris 光影下**完全不渲染**：Iris 只把原版管线映射到 shader pack 的 `gbuffers_*`（其 `ShaderKey` 枚举中可见 `ENTITIES_SOLID`、`TERRAIN_SOLID`、`TEXTURED` 等对应项），自定义着色器 ID 不在映射表内，开启光影且禁用未知着色器时整块被丢弃。
>
> 改用 `entitySolid` 后映射到 `ENTITIES_SOLID` → shader pack 的 `gbuffers_entity_solid`，原版与光影下均有输出。
>
> **已知代价**：原版 `end_portal` 着色器在片元里做多层视差采样，形成随视角滑动的星空隧道；标准管线只有一次采样，观感退化为按世界坐标连续平铺的静态星空贴图（无视差纵深）。此外传送门会接受 shader pack 对 `gbuffers_entity_solid` 的光照/PBR/阴影处理。这两点是获得光影可见性的必然取舍，已由实机确认可接受。

---

## 四、群系构成：原版群系剔除 + BWG 接管

这是天灾维度设计中最需要理解的一环。

### 4.1 目标状态（第二阶段已完成）

| 维度 | 群系构成 |
|---|---|
| 主世界 `minecraft:overworld` | **纯净原版**群系，BWG 完全不介入 |
| 天灾 `beloong:disaster` | **BWG 群系 + 5 个 `beloong:` 自制群系，零 `minecraft:` 群系** |

**5 个自制群系**（`src/main/resources/data/beloong/worldgen/biome/`，
均为原版对应群系的逐字复制——群系 JSON 不含自我 ID，换文件名即为新群系）：

| 自制群系 | 复制自 | 覆盖的原版项 | 参数点 |
|---|---|---|---|
| `beloong:frozen_ocean` | `minecraft:frozen_ocean` | `frozen_ocean` `deep_frozen_ocean` | 4 |
| `beloong:ocean` | `minecraft:ocean` | `cold_ocean` `deep_cold_ocean` `ocean` `deep_ocean` | 8 |
| `beloong:river` | `minecraft:river` | `river` `frozen_river` | 18 |
| `beloong:caves` | `minecraft:lush_caves` | `lush_caves` `dripstone_caves` `deep_dark` | 3 |
| `beloong:windswept` | `minecraft:windswept_hills` | `windswept_hills` `windswept_gravelly_hills` `windswept_forest` | 352 |

**交给 BWG 的 7 项**：`lukewarm_ocean` / `deep_lukewarm_ocean` → `lush_stacks`；
`warm_ocean` → `dead_sea`；`stony_shore` → `basalt_barrera`；
`windswept_savanna` → `araucaria_savanna`；`stony_peaks` → `red_rock_peaks`；
`snowy_beach` → `dacite_shore`。
**原有的 32 项气候对照**继续由 BWG 群系接管。全部 53 个原版群系均已覆盖。

> **参数点账目**（实测）：`7593 = 替换 7593 + 非原版保留 0 + 未能求解 0`。
> 其中 385 点指向自制群系，7208 点指向 BWG 群系（**BWG 占 94.9%**）。

**两个层面的目标必须同时达成：**

1. **生成层** —— 这些群系不再在天灾维度生成
2. **查询层** —— 它们从 `BiomeSource.possibleBiomes()` 中消失，因此 `/locate biome` 搜不到、
   自然罗盘只把它们显示在主世界

> 这两层是**独立的两条路径**，必须分别处理。这是本功能实现过程中最大的坑，详见
> `docs/天灾维度群系剔除-两次尝试复盘.md`。
>
> **第二阶段又发现第三条路径**：BWG 自己的 **region 树**也会直接吐出原版群系，
> 且 `findValuePositional` **先查 region 树**。见 10.6。

> **⚠️ 本功能没有任何配置项。** 白名单与映射表都是硬编码的结构性决策。
> 早期版本曾有 `[disaster_biomes]` 配置节（`enabled` 开关 + `fallbackBiome` 兜底群系），
> 已按设计决定**整体移除**——详见 4.4 与第八节决策 19。

### 4.2 实现机制：精准排除主世界

`data/terrablender/tags/dimension_type/overworld_regions.json`：

```json
{
  "replace": true,
  "values": [
    "beloong:disaster"
  ]
}
```

TerraBlender 通过 `LevelUtils.getRegionTypeForDimension()` 查询维度类型 tag `terrablender:overworld_regions`，命中才返回 `RegionType.OVERWORLD` 并执行群系注入。TerraBlender 自带的同名文件是 `replace: false` + `minecraft:overworld`。

**原理**：原版 `TagLoader` 在加载每个数据包的同名 tag 文件时：

```java
TagFile tagfile = TagFile.CODEC.parse(...).getOrThrow();
if (tagfile.replace()) {
    list.clear();          // 清空此前所有数据包累积的条目
}
tagfile.entries().forEach(e -> list.add(new EntryWithSource(e, sourcePackId)));
```

`replace: true` 会**清空列表**，从而抹掉 TerraBlender 自己注入的 `minecraft:overworld`。最终该 tag 只含 `beloong:disaster`：

- 主世界 → `getRegionTypeForDimension()` 返回 `null` → TerraBlender 直接 `return`，不做任何群系注入
- 天灾 → 返回 `RegionType.OVERWORLD` → 正常注入 BWG 群系

> ⚠️ **该文件是「一票否决」式的全局开关，改动它等于同时动两个维度。** 若要恢复主世界的 BWG 群系，需改为 `"replace": false` 并把 `minecraft:overworld` 加回 `values` 数组。

### 4.3 防污染：CloneParameterListMixin

`mixin/CloneParameterListMixin`（`@Pseudo`，目标 `terrablender.util.LevelUtils`）

天灾维度与主世界共用 `minecraft:overworld` 的 `multi_noise` 预设，而 TerraBlender 的 `initializeForTerraBlender` 是**在 `Climate.ParameterList` 实例上原地初始化**的。若不干预，初始化天灾维度会把共享的 `ParameterList` 标记为已完成初始化，导致 BWG 群系**泄漏回主世界**。

Mixin 用 `@Redirect` 拦截 `initializeBiomes` 中对 `IExtendedParameterList.initializeForTerraBlender(...)` 的调用：

```java
Climate.ParameterList cloned = (Climate.ParameterList) receiver.clone();
((IExtendedParameterList) cloned).initializeForTerraBlender(targetRA, regionType, targetSeed);
MultiNoiseBiomeSource biomeSource = (MultiNoiseBiomeSource) chunkGenerator.getBiomeSource();
((MultiNoiseBiomeSourceAccess) biomeSource).setParameters(Either.left(cloned));
```

**先 clone、再初始化、再写回**，使天灾维度持有独立的参数列表副本，主世界的共享实例保持未被污染。

TerraBlender 与 BWG 现为 **required 依赖**（见 `neoforge.mods.toml`），因此本 Mixin 不必再考虑
"TB 缺席"的情形；但仍保留 `@Pseudo` + `remap = false`（目标是 TB 类，TB 的 jar 不带 refmap）。

### 4.4 生成层剔除：在副本上改写群系

`CloneParameterListMixin` 在上述 clone 与 initialize **之间**插入了群系改写：

```
① cloned = receiver.clone()
② 先把 cloned.values 换成一个新列表        ← 断开与共享原件的 List 引用
③ 逐行改写：非 minecraft: 命名空间 → 保留
             命中白名单 21 项       → 保留
             其余 32 项             → 查 DisasterBiomeMapping 换成 BWG 群系
             映射不可用             → 保留原版群系 + 记 ERROR（无兜底群系）
④ cloned.initializeForTerraBlender(...)   ← index 0 兜底树此时才建立，读到的是改写后的行
⑤ biomeSource.setParameters(Either.left(cloned))
```

**为什么必须换新列表（②）**：`ParameterList.clone()` 是浅拷贝，副本与原件**共享同一个
`values` 对象**（TerraBlender 的 `MixinParameterList` 只把 `clone()` 重写为 `super.clone()`）。
原地改行会连主世界一起改。`values` 声明为 `private final`，故用
`@Mutable @Accessor`（`mixin/ParameterListAccessor`）整体替换——TerraBlender 自己改
`MultiNoiseBiomeSource.parameters` 这个 final 字段也是同样手法。

**为什么时机必须在 initialize 之前（④）**：TerraBlender 的 `MixinParameterList` 内部以
`Climate.RTree.create(this.values)` 构建 index 0 兜底树，而 region 树命中
`Region.DEFERRED_PLACEHOLDER` 时会回退到这棵树。晚于此处的任何修改都不会影响兜底树。

**维度守卫**：该改写点对**所有**被 TerraBlender 判定为 `RegionType.OVERWORLD` 的维度都会触发。
实测 `minecraft:the_nether` 也会命中（TerraBlender 按维度类型 tag 而非维度身份分支），
若不限定维度，下界 5 个群系会被换成本不应该出现的 BWG 地表群系。
故 `DisasterBiomeSubstitution.isTargetDimension(levelKey)` 只放行 `beloong:disaster`。

主世界不需要在此排除：**本模组用自己的 datapack** 以 `"replace": true` 覆盖了
`terrablender:overworld_regions`（见 4.2），主世界因此不在该 tag 内，
`LevelUtils.getRegionTypeForDimension()` 返回 `null`，`initializeBiomes` 在到达改写点**之前就已 return**。

> ⚠️ **不要把这个保护当成平台固有属性。** TerraBlender 自带的该 tag 内容是
> `{"replace": false, "values": ["minecraft:overworld"]}`——主世界被排除**完全是本模组覆盖的结果**。
> 任何在该 tag 里放回 `minecraft:overworld` 的数据包，都会让主世界也进入 `appendDeferredBiomesList`，
> 从而被查询层的过滤器命中（见 4.5 的判据说明）。

**降级保护（无兜底群系）**：改写前会校验目标群系「已注册」且「未被 BWG 配置禁用」
（BWG 的 `BWGTerraBlenderRegion` 会把被禁用的群系改写成 `DEFERRED_PLACEHOLDER`，
说明 BWG 自己都回避它们）。任一校验失败则**保留原版群系并记 ERROR**——
不使用兜底群系，因为"什么都不做"比"换成一个语义不搭的群系"更安全。

**异常安全**：替换段整体包在 `try/catch` 中。**本功能没有配置开关，没有逃生舱**，
所以失败时必须继续执行原有的 `initializeForTerraBlender` + `setParameters`（退化为 TB 原行为），
而不是让异常穿透 `LevelUtils.initializeOnServerStart` 的 level stem 循环——
那会导致该维度之后的所有 level stem 全部不再初始化。

### 4.5 查询层剔除：让 possibleBiomes 也干净

**这是与生成层完全独立的一条路径。** `/locate biome`、自然罗盘、地图读的都是
`BiomeSource.possibleBiomes()`。实测天灾维度关卡实际持有的 `BiomeSource` 是**另一个实例**
（其 `parameters()` 返回的 `ParameterList` 与 4.4 写入的 clone 不是同一个），
该实例的 `values` 从未被替换，因此 `possibleBiomes()` 曾报 **109** 个群系
（53 原版 + 56 BWG），其中 32 个是待剔除的。

**实现**：`mixin/PossibleBiomesFilterMixin`（目标 `BiomeSource`）——
原版构造器已把 `possibleBiomes` 字段包成 `Suppliers.memoize`，TerraBlender 的
`MixinBiomeSource.appendDeferredBiomesList` 又会把它整体替换为一个**新的缓存闭包**。
因此过滤必须发生在这个**缓存被建立的那一点**：在该方法 HEAD 处 `cancellable` 注入，
自行合并「现有集合 + 本次追加列表」、剔除黑名单、重设 supplier，然后 `ci.cancel()`。

> 反例记录：先尝试注入 `MultiNoiseBiomeSource.collectPossibleBiomes()` 的 RETURN 点，
> 日志确认注入触发、判据正确，但**完全无效**——缓存闭包建立后，`possibleBiomes()`
> 直接读缓存，不再经过被过滤的方法。

**维度识别用作用域标志**：读 `DisasterBiomeSubstitution.isFilteringTargetBiomeList()`，
由 `CloneParameterListMixin` 在 `LevelUtils.initializeBiomes` 调用 `appendDeferredBiomesList`
的**前后**用 `levelKey` 精确包夹（`beginTargetBiomeList()` / `endTargetBiomeList()`）。
另需 `isSubstitutionApplied()` 为真（生成层确实替换过）才过滤，保持两层一致。

> ⚠️ **这里曾有一个真实缺陷（2026-09-11 发现并修复）。** 原先只用全局的
> `isSubstitutionApplied()` 作判据。但 `LevelUtils.initializeOnServerStart` 会遍历
> **所有** level stem，而 **`minecraft:the_nether` 也会走到 `appendDeferredBiomesList`**
> （它同样有 TerraBlender 的 region 初始化）。处理完天灾后标志已为真，于是下界的
> 5 个原版群系被 `isBlocklisted` 一并过滤 ⇒ **下界结构集因 `hasBiomesForStructureSet`
> 预筛失败而整条剔除、`/locate biome` 失效**（生成层不受影响，只有查询层坏掉）。
> <p>
> **这个缺陷在第二阶段之前就已存在但被掩盖**：当时白名单非空，被过滤掉的下界群系
> 在账目里仍算"待剔除残留"，看起来像是设计如此；白名单清空后才暴露为明显的异常数字
> （下界 `possibleBiomes` 从 9 种掉到 4 种、`minecraft:` 归零）。
>
> **教训**：跨维度共享的**全局**标志不能用来做维度判定——必须用能在调用点精确判定、
> 且作用域受限的标志。这与决策 15（"维度识别用语义判据而非对象身份"）是同一类问题的两个面。

> **为什么不用"追加列表里是否含 BWG 群系"这个判据**（早期实现）：该列表来自**全局** region 表，
> 与"当前是哪个维度"无关。任何进入 `overworld_regions` tag 的维度其追加列表都含 BWG，判据恒真。
> 主世界当时之所以安全，靠的是本模组覆盖 tag（见 4.4 的警告），而不是该判据本身。

**保序要求**：写入的新 supplier 必须**保序**，因为 `possibleBiomes()` 的迭代顺序会进入
`ChunkGenerator` 的 `FeatureSorter.buildFeaturesPerStep`，决定 `PlacedFeature` 的全局编号，
进而影响 `setFeatureSeed` 与地物放置顺序。原版用 Guava `ImmutableSet`、TerraBlender 用
`ObjectLinkedOpenHashSet`，两者都保序；而 `Set.copyOf` 返回的 `ImmutableCollections$SetN`
其迭代顺序由每次 JVM 启动随机化的 `SALT32L` 决定，**不保序**。故用 `ImmutableSet.copyOf`
并**一次算好快照返回常量**（同时满足保序与记忆化）。

> **已知局限（刻意不修）**：该标志由服务端初始化时置位，因此**专用服务器的客户端不成立**，
> 那里会回退为修复前的行为（自然罗盘的列表会显示原版群系在天灾维度）。
> 生成与 `/locate`（服务端）不受影响。


### 4.6 为什么必须保留那 21 个原版群系

判据统一为「**BWG 在结构上不覆盖该参数区**」：

| 类别 | 数量 | 原因 |
|---|---|---|
| **海洋** | 9 | BWG 只在暖/热两列填了 `LUSH_STACKS` / `DEAD_SEA`，寒/冷/中性三列（占海洋 60%）主动留空 |
| **河流** | 2 | BWG **完全没有河流群系**——其全部源码内 `river` 零命中，也不声明 `minecraft:is_river` 标签。原版在 `EROSION[6] + coastContinentalness` 上独立注册 |
| **洞穴** | 3 | 原版洞穴群系占 `depth ≥ 0.2` 的参数区域，而**所有 BWG 群系（及所有地表群系）的 `depth` 都是 0**。用地表群系去填这些点，会导致地下直接生成地表群系 |
| **碎裂地形** | 3 | BWG 的 `SHATTERED_BIOMES_BWG` 与 `SHATTERED_BIOMES_TERRABLENDER` 两个数组 **25 格全空**——BWG 明确把碎裂地形留给原版。原版这三个群系的侵蚀维固定在 `[4500,5500]`，正是碎裂地形的签名 |
| **暖带裸岩峰** | 1 | BWG 的 `PEAK_BIOMES_BWG` 对 WARM 带最干旱两格主动 `DEFERRED` 回原版 |
| **冰海滩** | 1 | BWG 的 `BEACH_BIOMES_BWG` ICY 行全 `DEFERRED`——BWG 没有冰海滩 |
| **其他硬编码位置** | 2 | `stony_shore` / `windswept_savanna` 由 `OverworldBiomeBuilder` 中**未被 TerraBlender 覆写**的私有方法产生（`addMidSlice` / `addLowSlice` / `maybePickWindsweptSavannaBiome`），BWG 在这两处从不放置自己的群系 |

**这 21 个是当前阶段的已知缺口，不是最终状态。** 后续计划用 `beloong:` 命名空间的
自制群系接管它们，见第十节。

> 判据的一致性说明：`stony_shore` 与 `windswept_savanna` 确实有"尚可的替身"
> （`basalt_barrera` 带 `beach`+`wasteland` tag；`firecracker_chaparral` 与 `araucaria_savanna` 同格），
> 把它们收进白名单是**刻意选择保守**——宁可暂时保留原版，也不接受语义不搭的替代。


### 4.7 表面规则与结构

- **表面规则**：TerraBlender 的 `NamespacedSurfaceRuleSource` 按群系命名空间分发规则。
  它是**两段式**而非二选一——BWG 规则返回 `null` 时才落到原版规则。
  因此 `biomeswevegone:*` 走 BWG 规则，白名单里的 `minecraft:*` 走原版规则，行为一致。
  这一机制也为后续自制群系自带地表规则留了口子（见第十节）。
- **结构**：BWG 结构经由 `NoiseGeneratorSettings` 的 structure settings 生效，
  与主世界共享 `settings: minecraft:overworld`，行为一致。
- **群系筛选**：BWG 配置文件中被禁用的群系在天灾维度**不会被选为目标**
  （`DisasterBiomeSubstitution` 会检测到并**保留原版群系 + 记 ERROR**，无兜底群系）。
  注意：被禁用的 BWG 群系在 BWG 侧会被改写成 `DEFERRED_PLACEHOLDER`，
  即**退回原版兜底树**——在剔除生效后，兜底树里已是 BWG 群系，不再产出原版群系。
- **映射表回避 BWG 默认禁用的群系**：BWG 在 `BWGWorldGenConfig.getDefaultBiomes()` 里
  **硬编码禁用** `biomeswevegone:eroded_borealis`。若映射表的某个目标指向它，那么在未手工改过
  BWG 配置的环境上该映射会静默失效。因此映射表**刻意不使用它**——
  `grove` 改映射到默认启用的 `frosted_taiga`。**新增映射条目时必须核对目标的默认启用状态。**

### 4.8 实测确认

> **本节的数字分两个阶段**：一阶段基线（`替换 6782 + 白名单 811`）保留在下方供对照，
> 二阶段终值见 §10.0 与执行记录。

**生成层 —— index 0 兜底树**（`biomeSource.parameters().values`）：

```
关卡 biomeSource = MultiNoiseBiomeSource, parameters = Left(ParameterList)
关卡参数列表：共 7593 条，其中 BWG 6782 条、黑名单 0 条
```

**替换账目**（启动日志，第二阶段终值）：

```
[BeLoong] 天灾维度群系替换：参数点 7593 个 = 替换 7593 + 非原版保留 0 + 未能求解 0（替换生效）
```

逐点闭合。其中 385 点指向 `beloong:` 自制群系，7208 点指向 BWG 群系
（**BWG 占 94.9%**）。

**生成层 —— BWG region 树**（第二阶段新增的观测点：

```
[BeLoong] 天灾 region 树账目（minecraft: 应为空集）：
    minecraft:overworld：参数点 7593，DEFERRED 0，minecraft: 群系 53 种 [...]
    biomeswevegone:region_0：参数点 7593，DEFERRED 1534，minecraft: 群系 0 种 []
    biomeswevegone:region_1：参数点 7593，DEFERRED 2982，minecraft: 群系 0 种 []
    biomeswevegone:region_2：参数点 7593，DEFERRED 2990，minecraft: 群系 0 种 []
```

> `minecraft:overworld`（= `DefaultOverworldRegion`，index 0）的 `addBiomes` 输出**仍然是完整
> 53 个原版群系**，这是正常的：index 0 的**树**由 `this.values`（我们改写过的副本）建立
> （`MixinParameterList.java:79-82`），而它的 `addBiomes` 输出只喂给查询层的追加列表，
> 那一层由 `PossibleBiomesFilterMixin` 负责过滤。

**生成层 —— 真实区块生成**（第二阶段的补充实测）：

用临时数据包函数 `forceload` 强制生成天灾维度区块，**区块真的生成了**
（`region/r.-2.-2.mca` 94 KB 等含实际数据），全程无异常。
这证明自制群系与地表规则在**真实生成路径**上可用，而不只是注册表层面正确。

**查询层**（服务端启动后逐维度打印 `possibleBiomes()`，第一阶段基线）：

| 维度 | 白名单原版 | 待剔除残留 | BWG | 判定 |
|---|---|---|---|---|
| `minecraft:overworld` | 21 | 32 | 0 | ✅ 正确（主世界保留了全部 53 个原版群系） |
| `beloong:disaster` | **21** | **0** | 55 | ✅ 目标达成 |
| `minecraft:the_nether` | 0 | 5 | 0 | ✅ 未受污染 |
| `minecraft:the_end` | 0 | 5 | 0 | ✅ 未受污染 |
| `beloong:loong_palace` | 0 | 0 | 0 | ✅ 未受污染 |

> 主世界那一行的"21 + 32"只是把 53 个原版群系按本模组的白名单口径分了类，
> **不代表主世界被改动**——主世界仍是完整的 53 个原版群系（BWG = 0）。
> 第二阶段白名单清空后，天灾维度的 `minecraft:` 群系数为 **0**。

TerraBlender 仅为下界与天灾打印初始化行，主世界**从不出现**——与设计意图一致。

> ⚠️ **验证方法警告（两条）**
> 1. `tools/scan-biomes.js`（区块 NBT 扫描）**读数失真**：会把未生成群系的区块
>    （`Status=minecraft:structure_starts`）也算进去，且统计的是 palette 出现次数而非群系体积。
>    **不得用于"某群系是否生成"的判定。** 验证一律以**游戏内存**为准：
>    `ServerLevel.getBiome(pos)` 或 `chunk.getSection(i).getBiomes().get(x,y,z)`，
>    或反射调用游戏自己的 `ChunkSerializer.write` 后读 palette。
> 2. **旧区块的群系固化在 NBT 里、不会回填。** 用旧存档扫描必然得到假阳性——
>    加载旧区块还会刷新它的 `LastUpdate`，所以连"最后更新时间"都不能用来区分新旧。
>    **判据必须是"在从未探索过的区域新生成的区块"。**
> 详见 `docs/天灾维度群系剔除-两次尝试复盘.md` 第 3.4 节。


---

## 五、结构效果与禁空联动

天灾维度承载 Cataclysm 与 FDBosses 的高难度结构。进入这些结构范围时对玩家施加**禁空**效果，限制龙类飞行能力。

数据文件：`data/beloong/beloong/structure_effects/disaster.json`

| 结构 | 效果 | amplifier | duration |
|---|---|---|---|
| `cataclysm:acropolis` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:ancient_factory` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:burning_arena` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:cursed_pyramid` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:frosted_prison` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:ruined_citadel` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:soul_black_smith` | `beloong:flight_ban` | 5 | 100t |
| `cataclysm:sunken_city` | `beloong:flight_ban` | 5 | 100t |
| `fdbosses:chesed_arena` | `beloong:flight_ban` | 6 | 100t |
| `fdbosses:geburah_arena` | `beloong:flight_ban` | 6 | 100t |
| `fdbosses:malkuth_arena` | `beloong:flight_ban` | 6 | 100t |

**注意：该文件按结构 ID 匹配，不含维度限定条件。** 文件名 `disaster.json` 只是归类约定；若同 ID 结构出现在其他维度，效果同样生效。目前这些结构仅生成于天灾维度，因此实际作用范围等价。

### 配套实现

- `structure/StructureEffectLoader` — Codec 驱动的 `SimpleJsonResourceReloadListener`，扫描 `beloong/structure_effects/*.json`，解析为 `Map<ResourceKey<Structure>, List<EffectEntry>>`
- `structure/StructureEffectHandler` — 触发时机：
  - `ServerTickEvent.Post`：仅在玩家**所在区块坐标变化**时检测（避免每 tick 全量扫描）
  - `PlayerChangedDimensionEvent` / `PlayerRespawnEvent` / `PlayerLoggedInEvent`：清缓存并立即检测
  - `MobEffectEvent.Expired` / `Remove`：对 `Config.StructureEffects.watchedEffects` 中的效果，过期或被牛奶/指令移除时重检，若仍应生效则取消移除（防闪烁）
  - 命中判定：玩家 AABB 与结构 `StructureStart` 包围盒求交
  - 支持可选的 `advancement` 条件（进度已完成则跳过该效果）

### 效果本体

`registry/FlightBanEffect` — 每级降低 1 点 `dragonsurvival:flight_level`：

- 属性修饰符由 `ModMobEffects` 注册时通过 `addAttributeModifier` 挂到该属性上，原版按 `amount × (amplifier + 1)` 自动缩放
- `onEffectStarted()` 在修饰符生效后检查：若飞行等级 `< 0` 且玩家正在展翅，**强制收翅**并向所有追踪该玩家的客户端同步 `SyncWingsSpread`，同时发送 `message.beloong.flight_banned`
- 效果结束后**不自动恢复飞行**，玩家需手动按 G 键重新展翅
- 与 `BROKEN_WINGS` 的区别：`BROKEN_WINGS` 无条件收翅；禁空仅在等级降至负数时才收翅，保留了「低等级禁空只削弱悬停、不阻止飞行」的梯度

> ⚠️ **待复核项**：`flight_ban` 的等级 5/6 对应 `-6 / -7` 飞行等级。请对照《飞行等级系统》确认 DS 侧 `flight_level` 的实际量程——若上限远低于此，则该效果的梯度差异在实际游戏中不可观测，只剩下「强制收翅」这一行为。

---

## 六、配置参考

### 天灾群系剔除：**无配置项**

> **本功能没有任何配置项。** 白名单（21 项）与映射表（32 项）都是硬编码的结构性决策，
> 由参数空间的形状决定，不适合暴露成可自由编辑的配置项——改错一项会让地下生成地表群系，
> 或让某个气候带出现语义完全不符的群系。

| 想改的东西 | 改哪里 |
|---|---|
| 白名单（保留哪些原版群系） | `worldgen/DisasterBiomeSubstitution.WHITELIST` |
| 原版 → BWG 的对应关系 | `worldgen/DisasterBiomeMapping.substitute()` |
| 无 | ~~配置开关~~ / ~~兜底群系~~ |

**求解顺序**（无兜底）：映射表命中且目标可用 → 替换；否则 → **保留原版群系 + 记 ERROR**。

> 早期版本曾有 `beloong-common.toml` 的 `[disaster_biomes]` 节（`enabled` 开关 +
> `fallbackBiome` 兜底群系），已**整体移除**。移除理由见第八节决策 19。

### `beloong-server.toml` → `[disaster_portal]`

| 键 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `eyeItems` | `List<String>` | 见下 | 激活传送门所需的 12 种眼球物品 ID。**列表第 N 项 → 槽位 N+1**，顺序决定纹理。校验器仅要求字符串含 `:` |
| `teleportCooldownTicks` | `int` | `100` | 传送冷却，范围 `0`~`72000` |

默认 12 种眼球：

```
minecraft:ender_eye          cataclysm:mech_eye       cataclysm:flame_eye
cataclysm:void_eye           cataclysm:monstrous_eye  cataclysm:abyss_eye
cataclysm:desert_eye         cataclysm:cursed_eye     cataclysm:storm_eye
fdbosses:eye_of_chesed       fdbosses:eye_of_malkuth  fdbosses:eye_of_geburah
```

### `beloong-server.toml` → `[structure_effects]`

| 键 | 说明 |
|---|---|
| `watchedEffects` | 需要监听过期/移除事件的药水效果 ID 列表，用于禁空效果的重检防闪烁 |

### 不受配置控制的部分

| 项 | 值 | 位置 |
|---|---|---|
| 天灾维度 ID | `beloong:disaster` | `DisasterPortalBlock.DISASTER_DIM` |
| 5×5 框架形状 | 固定 | `DisasterPortalFrame.getCompletedPortalShape()` |
| 上下行判断依据 | 当前维度是否为 `beloong:disaster` | `DisasterPortalBlock.isDisasterDimension()` |
| 落点区块等待上限 | `200` tick（10 s，超时即报错放弃） | `DisasterPortalBlock.DESTINATION_WAIT_TIMEOUT_TICKS` |
| 落点预热方式 | `TicketType.PORTAL` 区域票据，半径 `0` | `DisasterPortalBlock.requestDestinationChunk()` |

---

## 七、资源清单

### Java

```
block/EyeType.java                        眼球槽位枚举
block/DisasterPortalFrame.java            框架方块（激活 / 去重 / 破坏）
block/DisasterPortalFrameEntity.java      框架 BlockEntity（eye_id NBT）
block/DisasterPortalBlock.java            传送门方块（原版 Portal 范式双向传送）
block/DisasterPortalBlockEntity.java      传送门 BlockEntity（渲染载体）
client/DisasterPortalRenderer.java        传送门 BER（标准实体管线）
client/DisasterPortalTransitionScreen.java 天灾维度「加载地形中」专用背景（NeoForge 过渡界面注册点）
mixin/CloneParameterListMixin.java        TerraBlender 参数列表隔离 + 生成层群系剔除
mixin/ParameterListAccessor.java          @Mutable @Accessor 暴露 final 的 values
mixin/PossibleBiomesFilterMixin.java      查询层剔除（改写 possibleBiomes 缓存）
worldgen/DisasterBiomeSubstitution.java   群系替换求解器（21 项白名单 + 有效性校验 + 生效标志）
worldgen/DisasterBiomeMapping.java        原版 → BWG 气候对照表（32 项 / 25 个目标）
structure/StructureEffectLoader.java      结构效果数据加载
structure/StructureEffectHandler.java     结构效果触发
structure/EffectEntry.java                效果条目
structure/StructureEffectEntry.java       效果条目 Codec
registry/ModBlocks.java                   方块 / BlockEntity 注册 + getEyeSlot()
registry/FlightBanEffect.java             禁空效果
```

### 数据包

```
data/beloong/dimension/disaster.json
data/beloong/dimension_type/disaster.json
data/terrablender/tags/dimension_type/overworld_regions.json    ← 主世界排除开关
data/beloong/beloong/structure_effects/disaster.json
data/beloong/loot_table/blocks/disaster_portal_frame.json
data/minecraft/tags/block/mineable/pickaxe.json                 （含 disaster_portal_frame）
data/minecraft/tags/block/needs_netherite_tool.json             （含 disaster_portal_frame）
```

> 群系剔除**不需要新的数据包文件**——白名单与对照表都在 Java 侧
> （`DisasterBiomeSubstitution.WHITELIST` 与 `worldgen/DisasterBiomeMapping`）。
> 映射表刻意**不使用** BWG 默认禁用的 `biomeswevegone:eroded_borealis`，
> 因此**不需要**手工调整 `config/biomeswevegone/world_generation.json`。
> 其他功能（天灾传送门）仍需既有资源，与本功能无关。

### 资源包

```
assets/beloong/blockstates/disaster_portal_frame.json     multipart，52 条条件
assets/beloong/blockstates/disaster_portal_block.json
assets/beloong/models/block/disaster_portal_frame.json    基础（无眼球）
assets/beloong/models/block/disaster_portal_frame_1..12.json
assets/beloong/models/block/disaster_portal_block.json
assets/beloong/models/item/disaster_portal_frame.json
assets/beloong/models/item/disaster_portal_block.json
assets/beloong/textures/block/disaster_portal_frame_side.png    共用侧面
assets/beloong/textures/block/disaster_portal_frame_bottom.png  共用底面
assets/beloong/textures/block/disaster_portal_frame_top.png     无眼球顶面
assets/beloong/textures/block/disaster_portal_frame_top_1..12.png
assets/beloong/textures/block/disaster_portal_frame_eye_1..12.png
assets/beloong/textures/disaster_portal.png                     传送门贴图（标准实体管线绘制）
assets/beloong/textures/item/disaster_portal_block.png
assets/beloong/lang/{zh_cn,en_us}.json
```

> 自研着色器资源（`assets/beloong/shaders/core/rendertype_disaster_portal.{json,vsh,fsh}`）已随 Iris 兼容改造一并删除。

### BlockState 模型匹配

使用 **multipart** 而非 variants：

- `HAS_EYE` 与 `EYE_TYPE` 是独立属性，Minecraft 会尝试全部 104 种排列
- multipart 中 `has_eye=false` 只需匹配朝向，自动覆盖 52 种「不可能」组合
- `has_eye=true` + 具体 `eye_type` 匹配对应眼球模型
- 未匹配的状态不渲染模型

### 多语言键

| 键 | 中文 | English |
|---|---|---|
| `block.beloong.disaster_portal_frame` | 天灾传送门框架 | Disaster Portal Frame |
| `block.beloong.disaster_portal_block` | 天灾传送门 | Disaster Portal |
| `block.beloong.disaster_portal_frame.eye_duplicate` | 该眼球已放置在传送门中 | This eye is already placed in the portal |
| `dimension.beloong.disaster` | 天灾 | Disaster |
| `beloong.configuration.disaster_portal` | 天灾传送门 | Disaster Portal |
| `message.beloong.disaster_portal.destination_timeout` | 传送失败：目标区块 %s, %s 未能加载，请稍后重试 | Teleport failed: destination chunk %s, %s could not be loaded |

---

## 八、设计决策记录

1. **纯数据包维度，无自定义 Java 维度类** — 复用原版主世界生成规则的最短路径，升级维护成本最低。
2. **`effects: minecraft:overworld`** — 天灾不需要自定义天空。把天空系统的影响范围严格限制在龙宫，避免两套维度机制相互干扰。
3. **用 `replace: true` 排除主世界而非 Mixin** — 在 tag 层做取舍是纯数据驱动方案，不引入对 TerraBlender 内部实现的耦合，也不需要 Mixin 即可达成「主世界退回原版」。
4. **`CloneParameterListMixin` 必须存在** — 主世界与天灾共用同一个 `multi_noise` 预设，TerraBlender 的原地初始化会跨维度泄漏。clone 是保证隔离的最小改动。
5. **数字槽位而非语义化眼球键** — 早期用 `"cataclysm:mech_eye"` 等短键导致枚举与硬编码 Map 绑定、配置形同虚设。数字槽位让配置列表与纹理解耦，任意物品 ID 均可工作。
6. **multipart 而非 variants** — variants 需穷举 104 种排列，multipart 靠条件匹配天然忽略非法组合。
7. **重实现渲染器而非继承 `TheEndPortalRenderer`/`TheEndPortalBlock`** — 原版渲染器在字节码层把 BlockEntity 硬转型为 `TheEndPortalBlockEntity`，无法子类化复用，只能重实现。（注：此处只说"必须自己写渲染器"，**不代表必须自研着色器**——用哪条管线是独立决策，见第 11 条。）
8. **上下行判断用「当前维度是否为天灾」** — 而不是判断「是否为配置的源维度」。这让任意维度（不只主世界）进入传送门都能去天灾，且下行逻辑无需区分来源。
9. **冷却写入玩家持久化 NBT** — 跨维度、跨重登保持，避免玩家在传送门两侧反复弹跳。
10. **框架破坏时用 `level.destroyBlock`** — 直接复用原版破碎音效与粒子，无需自行实现特效。
11. **渲染改用标准实体管线（Iris 兼容），代价是放弃视差隧道** — 最初实现为自研 `ShaderInstance`（`beloong:rendertype_disaster_portal`，`.vsh`/`.fsh` 照搬原版 `rendertype_end_portal`），在 Iris 光影下**完全不渲染**：Iris 只把原版管线映射到 shader pack 的 `gbuffers_*`，自定义着色器 ID 不在其映射表内，开启光影且禁用未知着色器时整块丢弃。改为 `RenderType.entitySolid()` 后映射到 `ENTITIES_SOLID`，原版与光影下均可见。
    - **代价**：原版 `end_portal` 着色器的多层视差采样（随视角滑动的星空隧道）无法在标准管线中复现，观感退化为按世界坐标连续平铺的静态星空贴图；且传送门会接受 shader pack 对 `gbuffers_entity_solid` 的光照/PBR/阴影处理。已由实机测试确认可接受。
    - **随之删除的死代码**：`BeLoongCoreClient.disasterPortalShader` 字段、`onRegisterShaders` 事件处理器、`assets/beloong/shaders/core/rendertype_disaster_portal.{json,vsh,fsh}`。改造后它们已无任何消费方。
    - **不引入 Iris 依赖**：全程只用原版 `RenderType`，因此不需要 `compileOnly`/`localRuntime` 任何光影模组，也不需要 `ModList.isLoaded` 守卫。
12. **在 TerraBlender「复印参数列表」的时机改写群系，而不是从源头重建** — 曾有一条更激进的路线
    （见 `disaster_test` 分支）试图在原版生成 `multi_noise` 对照表的 preset 阶段直接重建，
    用 7 个 Mixin 注入点全部失败。根因是该时机**极早**：早于任何维度上下文（判断不了维度身份）、
    早于 Forge 配置加载（读配置直接 NPE），且 `PUTFIELD` / `INVOKEDYNAMIC` 不是合法的注入点说明符。
    改为在 `LevelUtils.initializeBiomes` 复印副本的瞬间改写后，维度身份、注册表、配置全部就绪，
    且操作对象是副本，主世界天然隔离。完整复盘见 `docs/天灾维度群系剔除-两次尝试复盘.md`。
13. **生成层与查询层必须分别处理** — 两者是独立路径：生成走 `getNoiseBiome → findValuePositional`
    （用 region 树 + 兜底树），查询走 `possibleBiomes()`（由 `values()` 现算）。
    只修生成层时，区块生成已干净，但 `/locate biome` 与自然罗盘仍能搜到原版群系。
14. **查询层的过滤必须发生在「缓存闭包被建立」的那一点** — TerraBlender 的
    `appendDeferredBiomesList` 会把 `possibleBiomes` 换成一个缓存闭包，闭包在替换那一刻固化集合。
    过滤注入到 `collectPossibleBiomes()` 是无效的（该方法是缓存建立时调用一次，之后不再经过）。
15. **维度识别用语义判据而非对象身份** — 关卡实际持有的 `BiomeSource` 与 TerraBlender
    初始化时拿到的不是同一个实例（实测 `parameters()` 返回的 `ParameterList` 不同），
    因此按实例登记不可靠。最初改用「参数列表里是否含 `biomeswevegone:` 群系」这一语义判据。
    **该判据已废弃**（它依赖"主世界不含 BWG 群系"这一前提，且真正兜住它的是数据包 tag 覆盖），
    现行做法见决策 21。
16. **必须保留 21 个原版群系（分阶段完成）** — 判据是「BWG 在结构上不覆盖该参数区」：
    洞穴占 `depth ≥ 0.2` 而 BWG 群系全为 `depth = 0`；BWG 完全没有河流群系；
    海洋的寒/冷/中性三列 BWG 主动留空；`SHATTERED_BIOMES_*` 两个数组 25 格全空；
    `PEAK_BIOMES_BWG` 对 WARM 带、`BEACH_BIOMES_BWG` 对 ICY 带主动 defer；
    `stony_shore`/`windswept_savanna` 位于未被 TB 覆写的私有方法中。
    这 21 个留待后续用 `beloong:` 自制群系接管，见第十节。
17. **验证必须读游戏内存，不能依赖 NBT 扫描工具** — 本项目的 `tools/scan-biomes.js`
    会把尚未生成群系的区块（`Status=minecraft:structure_starts`）计入统计，且量纲为
    palette 出现次数而非群系体积，读数显著失真。曾因此一度误判实现失败。
    验证一律用 `ServerLevel.getBiome()` 或 `chunk.getSection(i).getBiomes()`；
    或反射调用游戏自己的 `ChunkSerializer.write` 读 palette。
    **且必须用"从未探索过的新区域"**——旧区块的群系固化在 NBT 里、不回填。
18. **BWG 配置读取用反射而非直接 import** — 直接引用使其方法在**类校验期**就解析 BWG 类，
    BWG 缺席时抛 `NoClassDefFoundError`——该错误发生在进入方法体之前，外层 `try/catch` 捕获不到，
    会导致 Mixin 应用失败。故改用 `Class.forName` + `ModList` 守卫，BWG 因此不需要 `compileOnly`。
19. **本功能不提供任何配置项** — 白名单与映射表是硬编码的结构性决策。移除早期
    `[disaster_biomes]` 配置节的直接原因：`enabled` 开关**只覆盖生成层、覆盖不到查询层**，
    导致"关闭"不仅没回到旧行为，反而制造出「群系照常生成、但 `/locate` 搜不到、
    结构集与地物被静默剔除」的第三种状态（因为过滤后的 `possibleBiomes` 会进入
    `ChunkGeneratorStructureState.hasBiomesForStructureSet` 与 `ChunkGenerator.applyBiomeDecoration`
    的 `retainAll`）。移除 `fallbackBiome` 的原因：它可按配置指向任意群系（包括黑名单里的原版群系），
    等于给"绕过白名单"开了后门；改为**不可用时保留原版并记 ERROR**，更安全也更简单。
20. **TerraBlender 与 BWG 改为 required 依赖** — 本功能的生成与查询两层都建立在
    TerraBlender 的注入之上（查询层的注入目标 `appendDeferredBiomesList` **只由 TB 提供**，
    原版 `BiomeSource` 没有这个方法），而全部映射目标都来自 BWG。
    在 `optional` 声明下，卸掉 TB 会从"功能失效"变成**启动崩溃**（`defaultRequire: 1`）。
    改为 required 与实际依赖关系一致，也顺带保证了 mixin 的应用顺序（两者都已有 `ordering="AFTER"`）。
21. **查询层与生成层共用「是否真的生效」标志** — `PossibleBiomesFilterMixin` 需
    `DisasterBiomeSubstitution.isSubstitutionApplied()` 为真才过滤，而不是自己判断替换是否发生。
    这样两层天然一致：替换没发生，查询层就不过滤。时序上安全——
    实测 `filter()` 比天灾的 `appendDeferredBiomesList` 早 426 ms。
    <br>⚠️ **2026-09-11 修正：该标志只能表达"是否生效"，不能表达"是哪个维度"。**
    维度判定必须另用**作用域标志** `isFilteringTargetBiomeList()`——后者由
    `CloneParameterListMixin` 在 `LevelUtils.initializeBiomes` 的调用点用 `levelKey`
    精确包夹。否则下界（同样会走到 `appendDeferredBiomesList`）会被误伤，
    其原版群系被过滤导致结构集被整条剔除。详见 §4.5 与决策 28。
22. **写入的新 supplier 必须保序且记忆化** — `possibleBiomes()` 的迭代顺序会进入
    `FeatureSorter.buildFeaturesPerStep`，决定 `PlacedFeature` 的全局编号，进而影响
    `setFeatureSeed` 与地物放置顺序。原版用 Guava `ImmutableSet`、TB 用 `ObjectLinkedOpenHashSet`，
    两者都保序；`Set.copyOf` 返回的 `ImmutableCollections$SetN` 其迭代顺序由每次 JVM 启动
    随机化的 `SALT32L` 决定，**不保序**。同时原版用 `Suppliers.memoize` 包裹该字段，
    重建缓存时若不重新包裹就丢失了记忆化。故用 `ImmutableSet.copyOf` 并一次算好快照返回常量。
23. **无配置项 ⇒ 异常安全是必需而非可选项** — 没有 `enabled` 开关意味着没有逃生舱，
    所以 `cloneBeforeInit` 的替换段必须 `try/catch`，失败时**继续**执行原有的
    `initializeForTerraBlender` + `setParameters`（退化为 TB 原行为）。
    若不捕获，异常会穿透 `LevelUtils.initializeOnServerStart` 的 level stem 循环，
    导致该维度之后的所有 level stem 全部不再初始化。
24. **天灾维度的群系绝不能是原版群系本身** — 这是两条总目的（1.1）的共同手段。
    Minecraft 的标签按**群系 ID** 挂载，把原版群系复制成 `beloong:` 后**不继承任何原版标签**，
    于是：原版结构在这些参数区自然失去生成条件（服务于目的 2），
    而目的 1（少自制）只需"复制 + 改名"。
    **推论：接管白名单项时，绝对不要把 `beloong:` 群系加入任何原版 `has_structure/*` 标签。**
25. **结冰与水体都不是群系的职责** — 水来自 `NoiseGeneratorSettings.default_fluid` + `sea_level`
    （`NoiseBasedChunkGenerator.java:71`）；结冰只看 `Biome.getTemperature(pos) >= 0.15F`
    （`Biome.java:139-173`）。二者都不涉及标签或硬编码群系 ID。
    这条推翻了「原版河流群系触发水体生成」这一早期顾虑（原 10.1 表已修正）。
26. **多个气候参数点可以指向同一个群系** — 「必须自制几个群系」由"想要几种观感"决定，
    而不是由参数空间里有多少个格子决定。这是 10.0 里 21 项压缩到 5–8 个的依据。
27. **原版结构在天灾维度的存在与否只由群系决定，但隔离本身不归本模组负责** —
    1.21.1 没有"按维度选结构集"的机制：`ChunkGenerator.createState` 把**全部** structure set
    交给每个维度，只按 `possibleBiomes()` 筛一道（`ChunkGeneratorStructureState.java:52-67`）。
    **结构隔离是整合包的目标**（§1.3），本模组不做。
    本模组唯一相关的义务是：接管白名单项时**不要**把 `beloong:` 群系加入任何原版
    `has_structure/*` 标签——那会破坏"复制品不继承原版标签"这一免费副产品。
28. **跨维度共享的全局标志不能用作维度判定** — `PossibleBiomesFilterMixin` 原先只读全局的
    `isSubstitutionApplied()`。但 `LevelUtils.initializeOnServerStart` 遍历**所有** level stem，
    而 `minecraft:the_nether` **同样会走到 `appendDeferredBiomesList`**。天灾处理完标志即为真，
    于是下界 5 个原版群系被一并过滤，结构集被 `hasBiomesForStructureSet` 整条剔除、
    `/locate` 失效（生成层无事，只坏查询层）。
    <br>**修法**：新增作用域标志 `isFilteringTargetBiomeList()`，由 `CloneParameterListMixin`
    在 `LevelUtils.initializeBiomes` 的调用点用 `levelKey` 精确包夹
    （`@Redirect` + `try/finally`），使标志只在**那一次调用**期间为真。
    <br>**为什么现在才发现**：第一阶段白名单非空，被误滤的下界群系在账目里仍算
    "待剔除残留"，看起来像设计如此；白名单清空后下界的 `minecraft:` 直接归零，
    异常立刻显形。**这也是补上查询层实测（§4.8）的直接收益。**
    <br>**教训**：与决策 15 同类——**"当前是哪个维度"必须用能在调用点精确判定的信号，
    不能靠跨维度共享的状态推断**。
29. **地表规则的每一条叶子都必须有 `ON_FLOOR` / `UNDER_FLOOR` 门** —
    `SurfaceSystem.java:124-153` 的循环遍历**整列**（从地表到 `getMinBuildHeight()`），
    对**每一个**等于 `defaultBlock`（石头）的方块都调用 `tryApply`，命中非 null 就替换。
    因此一条"末级恒不为 null"的规则若不加门，会把**整个地下**换成该材质。
    <br>天灾维度的 `beloong:windswept` 曾因照抄原版规则体却丢掉外层的
    `ifTrue(UNDER_FLOOR, ...)`（原版该阶梯挂在 `SurfaceRuleData.java:280` 之下）
    而把整片地下变成砂砾——**该群系占 352 / 7593 个参数点**。
    现已用 `surfaceWindow(rule)` 给每一级套上 `ON_FLOOR` + `UNDER_FLOOR`。
    <br>**范例**：BWG 的 `RED_ROCK_PEAKS` / `RUGGED_BADLANDS` / `REDWOOD_THICKET` 等
    每条叶子都被门住，是本项目应当照抄的写法。**新增任何地表规则时按此自检。**
30. **并行事件里写共享集合必须 `enqueueWork`** — `FMLCommonSetupEvent` 是并行分发的，
    而 `SurfaceRuleManager` 内部是普通 `HashMap`（`SurfaceRuleManager.java:35`）。
    地表规则注册改为包在 `event.enqueueWork(...)` 内，与 BWG 的既有做法一致
    （`BiomesWeveGoneNeoForge.onInitialize`）。丢失更新会让规则被**静默丢弃且不报错**，
    正是本功能最怕的失效模式。
31. **传送门改用原版 `Portal` 范式，主线程绝不等待区块** — 旧实现在 `entityInside` 里
    `targetLevel.getChunk(...)` **跨维度同步加载**，主线程会停在 `ServerChunkCache.getChunk` 的
    `managedBlock` 上自旋；一旦目标区块的 future 因异常逃逸而永不完成（实机：结构排除区递归
    使 worker 抛 `StackOverflowError`——环为 `beloong:disaster_set_ground` ⇄
    `beloong:disaster_set_underground` 互写 `exclusion_zone`，整合包侧已修复），主线程就**永久静默卡死**（无 crash report）。
    <br>**改法**：`entityInside` 只 `setAsInsidePortal` 登记；落点由 `getPortalDestination` 用
    **非阻塞** `getChunkNow` 探测 + `TicketType.PORTAL` 区域票据预热 + **下一 tick 重试** +
    `200 tick` 有界超时求得，换维度交给 `DimensionTransition`。
    <br>**两个必须写下来的坑**：① 原版 `handlePortal` **先设传送冷却再问落点**，而
    `setAsInsidePortal` 在冷却期只刷新冷却、不登记 `insidePortalThisTick` —— 返回 `null` 前
    **必须** `setPortalCooldown(0)`，否则玩家站在门里永远拿不到重试机会；
    ② `ServerChunkCache#getChunkFuture` **在主线程调用也会 `managedBlock`**（只有非主线程才异步），
    所以「异步 future + thenAccept」这条备选路线必须把调用派发到非主线程才成立。
    <br>**被否**：固定倒计时后再同步 `getChunk`（只是推迟卡死）、只探测不申领票据（区块永不加载）、
    用 `Level#getHeight`（未加载时仍会阻塞）。取证与实机协议见
    [`docs/天灾传送门主线程死锁-根因与修复复盘.md`](天灾传送门主线程死锁-根因与修复复盘.md)。
32. **天灾门采用原版「局部过渡」+ 专用过渡界面（2026-09-12 增补）** — 方块在**客户端也登记**
    `setAsInsidePortal`（与原版 `NetherPortalBlock` 同构：登记在两侧都做，而 `handlePortal()` 内含
    `ServerLevel` 判断，故客户端不会传送），覆写 `getLocalTransition()` 返回 `Portal.Transition.CONFUSION`
    ⇒ 获得下界门同款的镜头扭曲、`PORTAL_TRIGGER` 环境音与"自动关闭容器界面"。
    进入/离开 `beloong:disaster` 时客户端显示的「加载地形中」界面改用 NeoForge 的
    `RegisterDimensionTransitionScreenEvent`（`registerIncomingEffect` + `registerOutgoingEffect`）注册
    `client/DisasterPortalTransitionScreen`：背景铺 `beloong:textures/disaster_portal.png` + 轻压暗
    （原版 `ReceivingLevelScreen#renderBackground` 只对下界/末地提供专用背景，其余维度一律"模糊后处理 + 菜单底图"）。
    <br>**已知取舍**：扭曲的全屏叠色贴图由原版 `Gui#renderPortalOverlay` **硬编码**为下界门的粒子图标，
    无法替换；要主题化必须 Mixin `Gui`（已评估，放弃）。设计文档：
    [`docs/plans/2026-09-12-disaster-portal-transition-design.md`](plans/2026-09-12-disaster-portal-transition-design.md)。

---

## 九、已知问题与后续可选项

| 项 | 说明 |
|---|---|
| 返回传送门结构方案已取消 | 早期设计中的「下行时自动生成返回传送门结构」**未实现也不需要**——上行改为照搬原版末地返回逻辑（直接回重生点），无需预生成结构。因此不存在 `return_portal.nbt` 之类的天灾专用结构资源。（`data/beloong/structure/` 下的 `end_return_portal_{activated,deactivated}.nbt` 属于**末地返回传送门**功能，与天灾无关。） |
| **天灾维度结构排除区递归（已查明 / 已修复）** | 实机反复出现 `java.lang.StackOverflowError`（`StructurePlacement$ExclusionZone.isPlacementForbidden` ⇄ `ChunkGeneratorStructureState.hasStructureChunkInRange`，**≥1024 帧**，被 HotSpot 默认 `MaxJavaStackTraceDepth` 截断）。**真凶**：两个天灾专用结构集**互写** `placement.exclusion_zone` —— `beloong:disaster_set_ground` ⇄ `beloong:disaster_set_underground`（各 `chunk_count=10`），构成 2 环 → 结构放置无限递归。因环的入口要求该结构集出现在当前维度的 `possibleStructureSets` 里，而这两个 `beloong:` 集只在天灾维度满足群系条件，故**只有天灾维度炸、主世界正常**。**整合包侧已于 2026-09-12 19:54:14 删除这两处 `exclusion_zone`**；修复后用本仓库脚本复扫为 **0 环**。模组侧只保证：不再同步等待区块，生成失败最多退化为"200 tick 超时 + 报错 + 玩家提示"。对照原文、复核命令与"初版扫描为何漏掉该环"（扫描恰好跑在修复之后）见 [`docs/reviews/2026-09-12-structure-exclusion-recursion-probe.md`](reviews/2026-09-12-structure-exclusion-recursion-probe.md)。 |
| ~~同类阻塞写法仍在别处~~ | **已修复（2026-09-12）**：`transport/DimensionTransportHandler.java:126/172` 与 `ability/TpLoongPalaceEffect.java:81/119` 四处旧写法（`getChunk` 强制加载 + `Level#getHeight`）已改为零状态纯函数 `util/LandingY.resolveOrFallback`（非阻塞探测；区块未加载即用兜底 Y），并删除 `DimensionTransportHandler:172` 那行"结果从未被使用"的加载。落点区块由 `ServerPlayer#teleportTo(ServerLevel, …)` 自带的 `TicketType.POST_TELEPORT` 票据负责加载。当时验收使用的「已加载」路径落点公式保持逐字不变；`owToLP_fallbackY` 默认值按实测（龙宫落点地表 Y=64.0）由 `64.5` 对齐为 `65.0`。设计文档：[`docs/plans/2026-09-12-dimension-transport-nonblocking-design.md`](plans/2026-09-12-dimension-transport-nonblocking-design.md)。 |
| 传送门落点失败无自动兜底 | 落点区块 200 tick 内未就绪时只报错 + 提示，**不换落点、不预生成**。若某 XZ 命中上面的结构递归毒点，该点的传送功能即不可用（服务器仍存活）。 |
| `structure_effects/disaster.json` 无维度限定 | 按结构 ID 匹配。若将来这些结构 ID 也出现在其他维度，禁空效果会一并生效。 |
| `flight_ban` 等级量程 | 见第五节待复核项（`flight_level` 量程为 `[-1024, 1024]`，故 amp 5/6 = `-6/-7` 的梯度是可观测的）。 |
| 主世界群系 | 主世界当前为纯净原版群系，这是设计意图。若某天需要让部分 BWG 群系回到主世界，**只改 `overworld_regions.json` 是不够的**——必须同时处理两点：① 查询层的判据要改为按维度身份打标记（当前的「生成层是否生效」标志会让主世界在替换发生后也被过滤）；② 重新评估 `SHATTERED_BIOMES` 等 BWG 在结构上不覆盖的参数区。该 tag 文件是「一票否决」式的全局开关，改动等于同时动两个维度。 |
| ~~**21 个白名单原版群系（待处理）**~~ | **已完成**。14 项由 5 个 `beloong:` 自制群系接管，7 项交给 BWG。逐项方案见 [`docs/plans/2026-09-11-disaster-phase2-biome-table.md`](plans/2026-09-11-disaster-phase2-biome-table.md)。 |
| ~~**⚠️ 5 项黑名单群系仍在生成（第一阶段残留）**~~ | **已修复**。`badlands` / `eroded_badlands` / `wooded_badlands` / `mushroom_fields` / `beach` 曾由 BWG 的 region 树原样吐出（生成层仍产出、仅被查询层隐藏，构成决策 19 判定不可接受的「第三种状态」）。第二阶段的第三条注入路径已把它们一并改写。证据：`docs/reviews/2026-09-11-disaster-region-tree-probe.md` |
| 原版结构仍在天灾维度生成 | **不是本模组的问题**——整合包的目标，由整合包层面处理。相关事实与边界见 §1.3。本模组只需保证不给 `beloong:` 群系挂原版 `has_structure/*` 标签。 |
| **4.8 验证方法有盲区** | 4.8 读的 `biomeSource.parameters().values` 是 **index 0 兜底树**，完全不覆盖 region 树。这正是 5 项泄漏长期未被发现的原因。接管 region 树桶时必须补一条针对 region 树的验证。 |
| 映射表为手写最近邻 | `DisasterBiomeMapping` 的 32 项对应关系是按气候中心手写的，已覆盖原版参数空间全部 53 个群系的划分（21 白名单 + 32 映射）。若实测发现某替代群系出现在气候不协调处，可改为从 `run/exported_vanilla_biomes.json` 自动计算最近邻。**注意该指标对跨度大的群系有假阳性**——"格命中"（温度带×湿度档重合）应优先于中心距离。 |
| BWG 版本 | 由 `7745142` 升至 `8245253`（= BWG 2.6.0），与实机运行版本对齐。 |
| ~~`eroded_borealis` 需手动启用~~ | **已解决**：映射表原先 `grove → eroded_borealis`，而 BWG 在 `BWGWorldGenConfig.getDefaultBiomes()` 里**硬编码禁用**后者，导致干净部署上该映射静默失效。现改为 `grove → frosted_taiga`（BWG 默认启用），不再依赖整合包手工改 BWG 配置。**新增映射条目时必须核对目标的默认启用状态。** |
| 表面规则的分发边界 | `NamespacedSurfaceRuleSource` 按**群系命名空间**分发，且是**两段式**——BWG 规则返回 `null` 时才落到原版规则。因此天灾维度里白名单的 `minecraft:*` 群系走原版规则。这是当前的正确行为；待自制群系接管后，`beloong:` 命名空间可自带专属地表规则（见第十节）。 |
| 专用服务器客户端的自然罗盘列表 | 查询层的过滤标志由**服务端**初始化时置位，因此专用服务器的客户端不成立——那台客户端的自然罗盘列表会显示原版群系在天灾维度。生成与 `/locate`（服务端）不受影响。**这是刻意维持的现状，不修。** |
| 自然罗盘"搜索"不做预筛 | 自然罗盘的**列表**读 `possibleBiomes()`（已被正确过滤），但它的**搜索**用 `BiomeSearchWorker` → `getNoiseBiome` 螺旋扫描，没有"该群系是否可能存在"的前置判断，因此会白扫一圈才结束（扫不到黑名单群系，因为生成层已替换）。`/locate` 则走 `possibleBiomes` 预筛、立即返回——**实测证据**：在 `beloong:disaster` 里对 `minecraft:ocean` 等执行 `/locate biome` **无任何输出**（`ERROR_BIOME_NOT_FOUND`，`LocateCommand.java:131`），而 `beloong:ocean` 输出 `took 4 ms`（`:200`）。**不修**——扫描由 `WorldWorkerManager.IWorker` 分摊到多个 tick。 |
| ~~`possibleBiomes` 含 TerraBlender 哨兵~~ | **已修复**。BWG 的 region 树会把 `terrablender:deferred_placeholder`（`Region.DEFERRED_PLACEHOLDER`）写进输出列表，而它**确实注册在群系注册表里**，于是混进了 `possibleBiomes()`。它不是真实群系（`findValuePositional` 遇到它会回退 index 0 树，永不生成），但会让**自然罗盘把它列成一个可搜索群系**。查询层已加 `isDeferredSentinel(...)` 过滤；实测天灾的 `possibleBiomes` 由 61 种降为 **60 种**。 |
| `run/` 下的验证数据未版本控制 | `run/exported_vanilla_biomes.json`（映射表的气候依据）与 `run/config/biomeswevegone/world_generation.json` 都在 `.gitignore` 的 `run/` 内，仓库中无法复现。前者已被独立验证为真实的 MC 1.21.1 基线（逐点 7593/7593 吻合，含 float32 量化细节），数据本身没问题，只是可复现性欠缺。 |


---

## 十、第二阶段：用自制群系接管白名单 21 项（**已完成并通过验收**）

> **本节已结案**，保留作为实现依据与决策记录。**现行状态见 §1.3 与 §4.1**；
> 执行过程与验收证据见
> [`docs/plans/2026-09-11-disaster-phase2-implementation.md`](plans/2026-09-11-disaster-phase2-implementation.md)。
> **服务端账目核验与客户端观感验收均已通过**（2026-09-11）。
> 下面 10.0–10.6 记录的是**当时的判据与取舍**，其中 10.3「迁移路径」已由实际实施取代
> （多了一条 region 树注入路径，见 10.6）。

**第一阶段已完成**：天灾维度的 53 个原版主世界群系中，32 个已被替换为 BWG 群系，
查询层（`possibleBiomes()`）同步干净，`/locate` 查不到黑名单群系。
（**但有 5 个黑名单群系仍在生成层泄漏**，见 1.3 与
`docs/reviews/2026-09-11-disaster-region-tree-probe.md`。）

**第二阶段目标（已完成）**：把 21 个白名单原版群系全部接管，
使天灾维度**不含任何 `minecraft:` 群系**。

> **执行记录**：`docs/plans/2026-09-11-disaster-phase2-implementation.md`
> **逐项方案**：`docs/plans/2026-09-11-disaster-phase2-biome-table.md`

### 10.0 第二阶段的设计原则（已确认）

由 1.1 的两条总目的推导而来，作为后续所有选型的判据：

| # | 原则 | 推论 |
|---|---|---|
| 1 | **直接复制原版群系并改名为 `beloong:`，而非完全自制** | 复制品不继承任何原版标签 → 对整合包的结构隔离目标有正面贡献；工作量只是"复制 + 改名" |
| 2 | **自制群系数量尽可能少** | 自制群系每占一个气候区，BWG 群系的权重就少一分。**多个参数点可以指向同一个自制群系**——"必须自制几个"由"想要几种观感"决定，而不是由参数空间里有多少个格子决定 |
| 3 | **海洋数量尽可能压缩** | 原版 9 个海洋中只有 `frozen_ocean` / `deep_frozen_ocean` 温度是 `0.0`，其余 7 个**全是 `0.5`**；其差别仅在结冰、水色与地物。暖/热两列可直接交给 BWG |
| 4 | **洞穴数量尽可能压缩** | 3 个洞穴群系的温度（0.5 / 0.8 / 0.8）都不影响任何机制，合并只损失音乐、雾色与地物 |
| 5 | **底线：天灾维度绝对不能出现原版群系** | 不得为"减少自制"而保留任何原版群系 |

**定案：自制 5 个群系**（逐项依据见
[`docs/plans/2026-09-11-disaster-phase2-biome-table.md`](plans/2026-09-11-disaster-phase2-biome-table.md)）：

| 自制群系 | 取代 | 参数点 |
|---|---|---|
| `beloong:frozen_ocean` | `frozen_ocean` `deep_frozen_ocean` | 4 |
| `beloong:ocean` | `cold_ocean` `deep_cold_ocean` `ocean` `deep_ocean` | 8 |
| `beloong:river` | `river` `frozen_river` | 18 |
| `beloong:caves` | `lush_caves` `dripstone_caves` `deep_dark` | 3 |
| `beloong:windswept` | `windswept_hills` `windswept_gravelly_hills` `windswept_forest` | 352 |
| **合计** | **14 项** | **385** |

**其余 7 项交给 BWG**（426 点）：`lukewarm_ocean` / `deep_lukewarm_ocean` → `lush_stacks`；
`warm_ocean` → `dead_sea`；`stony_shore` → `basalt_barrera`；
`windswept_savanna` → `araucaria_savanna`；`stony_peaks` → `red_rock_peaks`；
`snowy_beach` → `dacite_shore`。

> ⚠️ **选 BWG 目标群系时的必查项：该群系有没有 BWG 地表规则。**
> BWG 只给 55 个群系中的 42 个写了规则（`BWGOverworldSurfaceRules.makeRules()`，
> `BWGOverworldSurfaceRules.java:434-480`）；没有规则的群系会落到原版规则，
> 而原版规则按 `minecraft:` ID 分支、一条都不命中 → 掉到默认**草/土**。
> 因此**地表本身就是其身份的群系**（如 `stony_peaks` 的方解石）绝不能指向无规则的 BWG 群系。
> 完整说明与已核对清单见逐项表 §6.2。

⇒ **最终 BWG 群系覆盖 7208 / 7593 点（94.9%）**，自制只占 385 点（5.1%）。

**群系标签：自制群系不挂任何原版标签**（`is_ocean` / `is_river` / `is_beach` / `is_hill` /
`is_mountain` / `is_savanna` / `is_overworld` / `has_structure/*` 全部不加），
**优先保证结构隔离**。代价（哪些效果可用群系自身数据补偿、哪些会真实丢失）见逐项表 §6.1。

**合并的代价（已接受）**：`beloong:ocean` 覆盖 4 个原版海洋，海底材质与刷怪无法再按原版粒度区分；
`beloong:caves` 覆盖 3 个洞穴，音乐、雾色、地表规则只能选一套；`beloong:river` 只做 1 个，冻河不再结冰。

**按参数点算的账（比"几个群系"更本质）**

原则 2 的真正杠杆不是"自制群系的**种类数**"，而是"必须由自制群系覆盖的**参数点数量**"——
一个自制群系可以覆盖很多参数点，所以少写几个群系并不会提高 BWG 的权重。
要让 BWG 权重真正上升，必须**把参数点交还给 BWG**。

原版参数空间共 7593 点，白名单 21 项占 811 点。逐项拆开（数字取自 index 0 兜底树的实测计数）：

| 组 | 白名单项 | 参数点 | 能否交给 BWG |
|---|---|---|---|
| 海洋（寒/冷/中性 6 项） | 6 | 12 | ❌ BWG 在寒/冷/中性三列主动留空 |
| 河流 | 2 | 18 | ❌ BWG 完全没有河流群系 |
| 洞穴 | 3 | 3 | ❌ BWG 没有任何 `depth > 0` 的群系 |
| **小计（真正的不可压缩核心）** | **11** | **33** | — |
| 海洋（暖/热 3 项） | 3 | 8 | ✅ BWG 的 `OCEANS_BWG` 在这两列本来就填了 `lush_stacks` / `dead_sea` |
| `stony_shore` | 1 | 12 | ✅ `basalt_barrera`（语义贴合） |
| `windswept_savanna` | 1 | 216 | ⚠️ `araucaria_savanna` / `firecracker_chaparral` |
| 暖带裸岩峰 | 1 | 80 | ⚠️ `red_rock_peaks`（**有** BWG 地表规则；`crag_gardens` 无规则已弃用） |
| 冰海滩 | 1 | 110 | ⚠️ `dacite_shore` / `basalt_barrera` |
| 碎裂地形 | 3 | 352 | ⚠️ 会失去"碎裂丘陵"地貌→ **已决定自制** |
| **小计（可谈判，已定案）** | **10** | **778** | — |
| **合计** | **21** | **811** | |

> **⇒ 真正的不可压缩核心只有 33 个参数点（占 7593 的 0.43%）**，对应
> **海洋 1–2 + 河流 1 + 洞穴 1 = 3–4 个自制群系**。
> 定案后多做一个 `beloong:windswept`（352 点）以保住碎裂丘陵地貌，合计 **5 个**。

> 附带事实：碎裂地形的暖/热两带**本来就由 BWG 自己的群系填充**——
> `SHATTERED_BIOMES_BWG` 第 3–4 行是 `Biomes.THE_VOID`，被 `BWGRegionUtils.java:28` 置为 `null` 后，
> `OverworldBiomeBuilder.java:993-996` 的 `pickShatteredBiome` 会回退到 `pickMiddleBiome`（BWG 群系）。
> 只有冰/寒/中性三带（第 0–2 行的 `DEFERRED_PLACEHOLDER`）才是缺口。

### 10.1 这 21 项为什么只能靠自制群系

> ⚠️ **本节原判据已被 2026-09-11 的取证修正。** 见 10.6：21 项在**机制**上分为两桶，
> 其中 7 项并不走 index 0 兜底树，**改映射表无法接管**。下表描述的是"BWG 为何给不出替代"，
> 仍然成立；但"如何接管"必须按 10.6 分桶处理。

见 4.6 节。13 项是**参数空间上的结构缺口**（BWG 根本没往那里放群系），
另有 2 项是**代码路径缺口**，还有 6 项是**刻意保守**（有替身但语义不搭，暂不收）：

| 类别 | 数量 | 缺口性质 | 自制群系的改造点 |
|---|---|---|---|
| 海洋 | 9 | BWG 只填暖/热两列，寒/冷/中性三列留空 | 需照顾结冰语义（`frozen_ocean` / `deep_frozen_ocean` 会结冰、有冰山） |
| 河流 | 2 | BWG 完全没有河流群系（源码内 `river` 零命中） | 需具备水体生成语义（原版河流群系触发水体/河床生成） |
| 洞穴 | 3 | BWG 无任何 `depth > 0` 的群系 | 需正确处理 `depth` 参数；地表规则可用命名空间隔离（见 10.2） |
| 碎裂地形 | 3 | BWG 的 `SHATTERED_BIOMES_*` 两数组 25 格全空 | 侵蚀维固定 `[4500,5500]`，需配套专属地表表现 |
| 暖带裸岩峰 | 1 | BWG 对 WARM 带最干旱两格主动 `DEFERRED` | — |
| 冰海滩 | 1 | BWG 的 ICY 海滩行全 `DEFERRED` | — |
| 其他硬编码位置 | 2 | `stony_shore` / `windswept_savanna` 由 TerraBlender **未覆写**的私有方法产生 | 优先改为交给 BWG（见 10.0） |

> **水与冰不需要自制群系做任何事**（已用源码核实）：
> - **水**来自 `NoiseGeneratorSettings.default_fluid` + `sea_level`，
>   与群系完全无关（`NoiseBasedChunkGenerator.java:71` → `Aquifer`）。
> - **结冰**只由群系温度驱动：`warmEnoughToRain` = `getTemperature(pos) >= 0.15F`
>   （`Biome.java:139-173`）。不涉及任何标签。
>
> 因此 10.1 表中"需具备水体生成语义"一栏是**多余的顾虑**。


### 10.2 关键手段：命名空间地表规则

`NamespacedSurfaceRuleSource` 是**两段式**而非二选一（`TerraBlender/.../surface/NamespacedSurfaceRuleSource.java:56-68`）：

```java
if (biome.is(key -> this.rules.containsKey(key.location().getNamespace())))
    state = this.rules.get(...getNamespace()).tryApply(x, y, z);
if (state == null)                     // ← 只有在上面返回 null 时才落到原版规则
    state = this.baseRule.tryApply(x, y, z);
```

`SurfaceRuleManager.addSurfaceRules(OVERWORLD, namespace, rules)` 接受任意命名空间。
**因此自制洞穴群系可以自带专属地表规则**，不必迁就原版——
这正是"必须保留原版洞穴群系"这一约束的解除路径。

### 10.3 迁移路径

> ⚠️ **原「两条注入路径无需任何改动」的说法只对 DEFERRED 桶的 14 项成立。**
> region 树桶的 7 项必须新增第三条注入路径，见 10.6。

**DEFERRED 桶（14 项）**：`CloneParameterListMixin` 生成层 / `PossibleBiomesFilterMixin` 查询层
**无需任何改动**——它们只认 `WHITELIST` 与 `DisasterBiomeMapping` 两个抽象：

1. 新建 `beloong:` 命名空间的群系（海洋 / 河流 / 洞穴可分批）并注册
2. 在 `DisasterBiomeMapping` 中把原版群系的对应项直接指向 `beloong:` 群系
   （注意 `isUsable` 是 `registry.getHolder(key).isEmpty()`，自制群系必须在**该注册表冻结前**注册好）
3. 从 `DisasterBiomeSubstitution.WHITELIST` 中**逐项移除**已接管的白名单条目
   （移除一项 ⇒ 该项从"白名单保留"转入"替换"，账目里的 `811` 会变小、`6782` 会变大）
4. 如需专属地表表现，注册 `beloong` 命名空间的 surface rule

**region 树桶（7 项）+ 第一阶段 5 项泄漏**：需要新增第三条注入路径（10.6），
在那之后同样复用第 2、3、4 步。

每接管一项都要重跑 4.8 的两条验证（关卡参数列表 + 日志账目），确保
`替换 + 白名单保留 + 未能求解 = 参数点` 仍然逐点闭合，且 **`未能求解 = 0`**。

> ⚠️ **4.8 的验证方法有一个已知盲区**：它读的 `biomeSource.parameters().values`
> 是 **index 0 兜底树**，**不覆盖任何 region 树**。这正是 5 项泄漏长期未被发现的原因。
> 接管 region 树桶的 7 项时，必须补一条针对 region 树的验证。

### 10.4 前置调研项

**已结案（2026-09-11）：**

1. ~~**自制群系的注册时机**~~ → **无问题**。`filter()` 跑在 `ServerAboutToStartEvent`
   （`IntegratedServer.java:76` / `ServerLifecycleHooks.java:98`），此时数据包注册表早已加载完毕；
   现有代码已成功对 `biomeswevegone:` 做 `registry.getHolder(...)`
   （`DisasterBiomeSubstitution.java:249`），对 `beloong:` 同理。
   用数据包 JSON 定义群系即可，不涉及 Java 注册时机。
2. ~~**水体语义**~~ → **多余的顾虑**。水来自 `NoiseGeneratorSettings.default_fluid` + `sea_level`，
   与群系完全无关（`NoiseBasedChunkGenerator.java:71`）。自制海洋/河流**自动有水**。
3. ~~**结冰语义**~~ → **只看温度数值**。`Biome.shouldFreeze` 由
   `warmEnoughToRain` = `getTemperature(pos) >= 0.15F` 决定（`Biome.java:139-173`），
   不涉及任何标签或硬编码群系 ID。自制冰海只要温度 ≤ 0.15。

**仍待验证：**

4. **碎裂地形的侵蚀维**——自制群系在 `[4500,5500]` 侵蚀区是否需要不同的噪声设置
5. **结构标签**——`structure_effects/disaster.json` 按**结构 ID** 匹配，与群系无关，
   预期不受影响。真正要注意的是反向的约束：接管 21 项时，`beloong:` 群系**不得**被加入
   任何原版 `has_structure/*` 标签——那会破坏"复制品不继承原版标签"这一免费副产品（§1.3）。
6. **地表规则**——原版 `overworld` 的 `surface_rule` 按**具体群系 ID** 分支
   （`SurfaceRuleData.java` 内 30 余处 `SurfaceRules.isBiome(...)`，如 `:69` `:74` `:82`
   `:85` `:88` `:117` `:119` `:127` `:178` `:221` `:289`）。换成 `beloong:` ID 后一条都不命中，
   会全部掉到默认的草/土/石——**碎裂地形会变成草原、裸岩峰会变成草坡、海洋底会从沙变砂砾**。
   ⇒ 必须注册 `beloong:` 命名空间地表规则（10.2 已提供手段）。
7. **游标成本**——白名单缩小意味着 `filter()` 的替换分支执行次数上升，需复测启动耗时
   （当前 `filter()` 实测约 426 ms）

**已知无法复刻（除非再写 Mixin）：**

8. **原版冰海的大冰架**——硬编码在 `SurfaceSystem.java:158-160`、`:232-275`，
   按 `Biomes.FROZEN_OCEAN || Biomes.DEEP_FROZEN_OCEAN` **群系身份**判定。
   小冰山是数据驱动的（`iceberg_packed` / `iceberg_blue` / `blue_ice`），可以照抄。

### 10.5 明确不要做的事

- **不要**为了"减少自制数量"而把语义**明显**不搭的 BWG 群系塞进白名单位置。
  （注：`stony_shore` / `windswept_savanna` 已在 10.0 中**改为交给 BWG**——
  它们有同格且语义尚可的替身，语义差距在可接受范围内；其余 19 项不适用此例外。）
- **不要**新增 `eroded_borealis` 这类目标——BWG 默认禁用，干净部署上会静默失效
- **不要**重新引入 `[disaster_biomes]` 配置项（决策 17：全部硬编码，无逃生舱）
- **不要**调整 `data/terrablender/tags/dimension_type/overworld_regions.json`——
  它是主世界/天灾共享的一票否决开关，改动等于同时动两个维度
- **不要**只改 `DisasterBiomeMapping` 就从白名单移除那 7 项（见 10.6）——
  那会得到"一半自制、一半原版"的空间混合，比不改更糟

### 10.6 21 项在机制上分两桶（2026-09-11 取证结论）

原「10.3 迁移路径：两条注入路径无需任何改动」的说法**只对其中 14 项成立**。
完整证据见 `docs/reviews/2026-09-11-disaster-region-tree-probe.md`。

`MixinParameterList.initializeForTerraBlender` 为**每个 region** 各建一棵 RTree
（`MixinParameterList.java:74-95`），而 `findValuePositional` **先查 region 树**，
只有拿到 `Region.DEFERRED_PLACEHOLDER` 才回退到 index 0 兜底树（`:144-149`）。
index 0 = `DefaultOverworldRegion`，其树由 `Climate.RTree.create(this.values)` 建立
（`:79-82`）——**那才是 `DisasterBiomeMapping` / `WHITELIST` 改写的那一份**。

| 桶 | 项数 | 项 | 来源 | 改映射表 |
|---|---|---|---|---|
| **DEFERRED 桶** | 14 | 海洋 9 + 碎裂 3 + `stony_peaks` + `snowy_beach` | BWG 数组在这些格子填 `DEFERRED_PLACEHOLDER` → 回退 index 0 | ✅ 有效 |
| **region 树桶** | 7 | `river` `frozen_river` `lush_caves` `dripstone_caves` `deep_dark` `stony_shore` `windswept_savanna` | 原版 `OverworldBiomeBuilder` 的私有硬编码路径，BWG 在 `BWGTerraBlenderRegion.java:165-168` **原样放行** | ❌ **无效** |

**⇒ 第二阶段需要新增第三条注入路径**：在 BWG 把 region 树输出交出去的那一刻改写参数对。
建议注入点：`BWGTerraBlenderRegion.addBiomes`（公开方法、签名稳定），
用 `@ModifyVariable(argsOnly = true)` 包裹传入的 `Consumer`——
**一处注入同时覆盖 RTree 构建与 `appendDeferredBiomesList` 列表两条路径**，因为两者都走 `region.addBiomes`。

**同一处注入顺带修掉第一阶段的 5 项泄漏**（`badlands` / `eroded_badlands` / `wooded_badlands` /
`mushroom_fields` / `beach`）——它们是黑名单却仍由 region 树生成，被查询层过滤器隐藏，
构成决策 19 判定不可接受的"第三种状态"。

> 其中 `beach` 尤其隐蔽：`BWGBiomeSelectors.java:104` 的 `BEACH_BIOMES_BWG[1]` 里
> BWG **自己写了 `Biomes.BEACH`**，于是它走 `bwgKeys` 分支，被当作"BWG 自己的群系"输出。


---

## 十一、已废弃文档

以下文档描述的设计已被本文件取代，已从仓库删除：

| 已删除文件 | 废弃原因 |
|---|---|
| `docs/天灾传送门.md` | 内容已并入本文件；其中记录的「下行自动生成返回传送门结构」与 `returnStructureTemplate` / `structureOffsetX/Y/Z` 配置从未落地，实际代码中不存在 |
| `docs/superpowers/specs/2026-06-02-disaster-portal-design.md` | 初版设计稿。含已废弃的 `Config.sourceDimension` / `disasterDimension` / `returnStructureTemplate` 配置项与结构模板放置逻辑 |
| `docs/superpowers/plans/2026-06-03-disaster-portal.md` | 上述设计稿的配套实现计划，随设计稿一并废弃 |
| `docs/superpowers/specs/2026-06-03-disaster-portal-config-fix-design.md` | 记录语义化眼球键 → 数字槽位的重构过程。重构结论已固化为现行实现，过程文档无保留价值 |
| `docs/superpowers/specs/2026-06-02-disaster-bwg-biomes-design.md` | 设计的 `BWGIntegration` / `DisasterBiomeRegion` / `BWGTerraBlenderRegionsMixin` **三个类均不存在于代码中**；实际采用 `overworld_regions.json` + `CloneParameterListMixin` 方案，见第四节 |
| `docs/superpowers/plans/2026-06-02-disaster-bwg-biomes.md` | 上述方案的配套实现计划（468 行任务清单），随方案一并废弃 |
| `docs/superpowers/plans/2026-06-16-disaster-portal-texture.md` | 纹理渲染的实现任务清单（施工步骤），随自研着色器方案一并废弃 |
| `docs/superpowers/specs/2026-06-16-disaster-portal-texture-design.md` | 自定义传送门贴图 + 自研着色器管线的设计说明。**Iris 兼容改造后该管线已被移除**（见第八节决策 11），三个着色器文件不再存在 |

### 相关文档与处置状态

| 文件 | 性质 | 状态 |
|---|---|---|
| `docs/superpowers/specs/2026-05-31-disaster-dimension-design.md` | 纯数据包维度定义的设计说明 | **现行有效**，与 `dimension/disaster.json`、`dimension_type/disaster.json` 一致 |
| `docs/天灾维度总设计.md`（本文件） | 天灾维度的**唯一权威文档** | **现行有效**，与其他文档冲突时以本文件为准 |
| `docs/plans/2026-09-11-disaster-vanilla-biome-removal-design.md` | 原版群系剔除功能的**设计文档**（第一阶段的施工依据） | **已实现、已验收**。保留其"注入点选择理由"部分；文中的 `[disaster_biomes]` / `fallbackBiome` / 14 项白名单均已作废，以第四节为准 |
| `docs/reviews/2026-09-11-disaster-biome-removal-review.md` | 实现后的**五组审查报告**（Mixin 正确性 / 映射白名单 / 一致性配置 / 性能 / 静态审查方法论） | **审查已完成、问题已全部修复**。报告描述的是修复前的代码状态，仅作过程记录保留 |
| `docs/天灾维度群系剔除-两次尝试复盘.md` | 原版群系剔除功能的**复盘文档** | **已结案**。`disaster_test` 七次失败与 `disaster2` 两次中途失败的完整记录，及可带走的规则 |
| `docs/plans/2026-09-10-disaster-pure-bwg-biomes-handover.md` | **历史交接文档** | **已结案**。其失败记录是「哪些注入点根本不可用」的权威清单（`PUTFIELD` / `INVOKEDYNAMIC` 不是合法注入点说明符、`@Invoker` 必然递归、preset 阶段读配置会 NPE），这些结论不会过时 |
| `docs/reviews/2026-09-11-disaster-region-tree-probe.md` | region 树输出的**消费端取证报告** | **现行有效**。证实 7 项白名单走 region 树、并查出 5 项黑名单泄漏。是「验证方法有盲区」这一结论的证据 |
| `docs/plans/2026-09-11-disaster-phase2-biome-table.md` | 第二阶段**逐项接管表**（四组分类 / 三档方案 / 复刻清单 / §6.2 地表规则必查项） | **现行有效**，第二阶段选型的依据 |
| `docs/plans/2026-09-11-disaster-phase2-implementation.md` | 第二阶段**实施计划 + 执行记录 + 验收清单** | **已执行完毕**（T0–T16、T18 通过）。另记录两轮中途修复（下界回归、独立审查发现的 Critical）与「可复用的验证手法」 |
| `docs/plans/2026-09-11-disaster-phase2-handover.md` | 第二阶段**交接文档** | **已结案**。其第一、二节的机制事实与职责边界仍可作维护背景 |

> **阅读顺序建议（第二阶段已完成）**：先读**本文件第四节**（现行机制）与 **§1.3**（职责边界），
> 需要改群系时读 `docs/plans/2026-09-11-disaster-phase2-biome-table.md`；
> 需要复现验证时读 `docs/plans/2026-09-11-disaster-phase2-implementation.md` 的
> 「验收标准」与「可复用的验证手法」两节。
> **两个阶段均已落地并通过验收**：
> - 第一阶段：32 项原版群系替换为 BWG 群系，5 项黑名单泄漏已随后续工作修复。
> - 第二阶段：21 项白名单全部接管，白名单清空，天灾维度零 `minecraft:` 群系；
>   服务端账目核验与客户端观感验收均通过。
> 后续如需改动，从上述文档入口进入即可。

> `docs/plans/2026-09-10-disaster-pure-bwg-biomes-design.md`（当时的配套设计文档）
> 位于 `disaster_test` 分支，**未合入 `disaster2`**——其设计已被本文件第四、十节取代。

原「仍然有效」的 `docs/superpowers/specs/2026-06-16-disaster-portal-texture-design.md`（自定义传送门贴图 + 着色器管线）**已随 Iris 兼容改造整体作废**——现行渲染器不再使用自研着色器，该文档描述的 `.vsh`/`.fsh` 管线与三个着色器文件均已不存在。详见第八节决策 11 与 3.7 节。
